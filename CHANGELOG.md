# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## 2026-05-20 — executor-async-rewrite(TBD-010 closed)

- BREAKING:`StepExecutor.execute` ABC 全切 `async def`(原 sync def + `asyncio.to_thread` 包装已删除)
- orchestrator 原生 `await executor.execute(ctx)` — `CancelledError` 直达 executor 内部,真停不需等 sync 线程
- ComfyAgentWorker:async-subprocess(`asyncio.create_subprocess_exec`)+ per-loop `_comfy_submit_lock` 串行锁 + cancel 时 server-side `/interrupt` + `proc.terminate()` 双保险
- 新 module `framework.runtime.lifecycle`:`ExternalProcessLifecycle` ABC + `ComfyLifecycleManager`(三模式 `none` / `ensure_running` / `ensure_release` / `self_managed_session`;A+seam 设计)
- orchestrator 持有 lifecycle manager:`arun` try/finally `_release_lifecycle_bounded(timeout=30s)` + `Orchestrator.aclose()` disposal 钩子
- cascade-cancel:真停 + `_CASCADE_DRAIN_TIMEOUT_S=30s` drain 超时明示失败(非 best-effort silently discard)
- `comfy_lifecycle`:四值受理(`none`/`ensure_running`/`ensure_release`/`self_managed_session`);集合外值才 `raise WorkerUnsupportedResponse`(原 `!= "none"` 即 raise 已解锁)
- `DryRunPass.run` 改 `async def` + 新 `ComfyAgentWorker.aprobe` async classmethod(Fluid Pause #1 scope 扩大)
- `StepContext.lifecycle: ExternalProcessLifecycle | None = None` 字段加入
- 全量 1179 passed / 3 skipped / 0 failed(实测 2026-05-20;baseline 1136 → net +43 新 fence)
- L2 live evidence:`docs/archive/forge_changes/2026-05-20-executor-async-rewrite/notes/live_smoke_lifecycle_20260520.md`

## [Unreleased]

### Changed

- **Backlog closeout rule**: `AGENTS.md` / `CLAUDE.md` / `docs/backlog/README.md` 现在要求凡是源自 `docs/backlog/active.md` 的任务,收尾时必须显式处理 active→archived 状态,小 bugfix 也不例外,避免只改代码不结账。
- **FOR-24 Linux runner CI**: 新增 `.github/workflows/linux-ci.yml` 的 `ubuntu-latest` job,安装 `.[dev,llm,server]` 后以 `python -m pytest -q` 作为唯一主门禁;同时把 `PyYAML` / `ruamel.yaml` / `beautifulsoup4` 显式列入运行时依赖,确保干净 Linux runner 能完整导入 `model_registry` 与 `pricing_probe`。
- **FOR-28 FBX self-containment validation**:`mesh_worker._is_self_contained_fbx` 新增 dependency-free `FileName` / `RelativeFilename` sidecar 扩展名扫描;`_build_candidate` 普通模式拒绝非自包含 `.fbx` 并沿既有 URL fallthrough / fallback,geometry-only 模式保留几何并标记 `missing_materials=True`。同步 retired active backlog `LR-0129` / `TBD-004`。
- **FOR-26 remote audio worker integration**:新增通用 `RemoteHttpAudioWorker`,通过 `FORGEUE_REMOTE_AUDIO_URL` + 可选 `FORGEUE_REMOTE_AUDIO_API_KEY` / `FORGEUE_REMOTE_AUDIO_MODEL` 注入 `GenerateAudioExecutor`;新增 `remote/audio` virtual model、`audio_remote` alias 和 `examples/remote_audio_smoke.json`。远端响应支持 base64 音频或 URL 下载,并对 `flac/mp3/wav` 做 magic bytes 二次校验。同步 retired active backlog `LR-0127` / `TBD-002`。
- **FOR-26 MiniMax music direct worker**:新增 `MiniMaxMusicWorker`,在未设置通用 `FORGEUE_REMOTE_AUDIO_URL` 且存在 `MINIMAX_KEY` 时注入 `GenerateAudioExecutor`;新增 `minimax/music-2.6` virtual model、`audio_minimax` alias 和 `examples/minimax_music_smoke.json`。worker 直连 MiniMax `music_generation`,发送原生 `model/prompt/lyrics/audio_setting/output_format` payload,支持 `url` 下载与 `hex` 音频响应,沿 audio worker `flac/mp3/wav` magic bytes 校验,并在 metadata 中去掉 MiniMax 临时签名 URL 的 query。
- **FOR-22 + FOR-23 SRS backlog closeout**:`DryRunPass` 现在校验显式声明的 provider `api_key_env`,缺 key 时阻断 Run;`Orchestrator` Step 异常失败路径 emit `step_failed` ProgressEvent,并携带 `exception_type` / `failure_mode` / `decision`。同步 retired active backlog `LR-0111` 与 `LR-0114`。
- **FOR-8 multi-mode comfy DAG warning**:`ManagedProcessRegistry.select` 不再只采用第一个
  managed subprocess lifecycle selection;同一 run 内多个 Comfy step 解析出不同 lifecycle mode
  时直接 `ValueError` fail-fast,避免 `ensure_running` / `ensure_release` 混用导致释放语义含糊。
  同步 retired active backlog `multi-mode-comfy-dag-warning`。
- **FOR-10 wait-ready-monotonic-time**:`ComfyLifecycleManager._wait_ready` 改用 `time.monotonic()` 绝对 deadline,新增 oversleep 回归测试;对应 active backlog `wait-ready-monotonic-time` 已归档。
- **FOR-11 blob backend streaming implementation**:`BlobBackend` 从
  `NotImplementedError` stub 升级为 MVP object-store backend,提供可注入
  `BlobClient` protocol + 默认 `InMemoryBlobClient`;支持 value/source_path 写入、
  read/exists、blob resume drift 校验,并允许
  `ArtifactRepository.put(source_path=..., payload_kind=PayloadKind.blob)`。同步
  retired active backlog `blob-backend-streaming-implementation`。
- **FOR-14 metadata corruption detection**:`ArtifactRepository.dump_run_metadata`
  现在在 `_artifacts.json` 旁写 `_artifacts.integrity.json`,记录 sha256 /
  artifact_count / artifact_ids;`load_run_metadata` 发现 integrity 文件时先校验,
  mismatch 抛 `ArtifactMetadataIntegrityError` fail-fast。legacy 无 integrity 文件
  run 保持兼容。同步 retired active backlog `metadata-corruption-detection`。
- **FOR-13 worker candidate source_path migration**:`ImageCandidate` / `MeshCandidate`
  / `AudioCandidate` / `VideoCandidate` 扩 `source_path`;本地 ComfyUI worker 只读格式校验所需
  文件头,由 generator executor 优先调用 `repo.put(source_path=...)` 落盘,`data`
  保留给 fake / 远端 worker 与兼容回退。同步 retired active backlog
  `worker-candidate-source-path-migration`。
- **FOR-12 repo.put staging hash atomicity**:`PayloadBackend.write` / registry write 返回
  `WriteResult(ref, content_hash)`;`FileBackend.write(source_path=...)` 改为先对
  staging `tmp_dest` 完成 size + `hash_path(tmp_dest)` 验证,再 `os.replace` 到 final
  dest,消除 replace 后 hash 失败导致 payload / metadata 半提交窗口。同步 retired
  active backlog `repo-put-staging-hash-atomicity`。
- **FOR-5 + FOR-6 FakeComfyWorker async/multimodal test fixture**:
  `FakeComfyWorker.agenerate` 现在真实让出 event loop;同一 fake worker 补
  `agenerate_mesh` / `agenerate_audio` / `agenerate_video` stub,并兼容 mesh executor
  远端注入路径的 `agenerate(source_image_bytes=...)`。同步 retired active backlog
  `fake-comfy-worker-agenerate-yield-point` 与 `fake-comfy-worker-mesh-audio-video-stub`。
- **项目工作流迁移**:ForgeUE_codex 主工作流从项目内 forge 切换为 Superpowers-first。长期 backlog / contracts / change archive 内容已复制到 `docs/backlog` / `docs/contracts` / `docs/archive/forge_changes`;旧 `forge/` 目录不由 Codex 删除,由用户按人工清单处理。

### Removed

- **ForgeUE-specific 协议层全 retire**(2026-05-10/11,OpenSpec change `retire-forgeue-protocol-layer-fully`,archived as `2026-05-10-retire-forgeue-protocol-layer-fully` commit `50cb543`):整 retire 9 个 `/forgeue:change-*` 命令(.claude/commands/forgeue/)+ 8 个 `tools/forgeue_*.py` 工具(forgeue_finish_gate / forgeue_change_state / forgeue_verify / forgeue_doc_sync_check / forgeue_subagent_budget / forgeue_skill_cascade_check / forgeue_enum_cross_ref_check / forgeue_env_detect)+ 2 个 sister skill(.claude/skills/forgeue-integrated-change-workflow + forgeue-doc-sync-gate)+ 3 个协议文档(docs/ai_workflow/forgeue_integrated_ai_workflow.md 整删 + forgeue_quickstart.md 整删 + docs/ai_workflow/README.md Documentation Sync Gate 段删)+ 17 个测试文件(tests/unit/test_forgeue_*.py + test_skill_cascade_check.py + test_followon_registry.py)+ 5 个 fixture(tests/fixtures/forgeue_workflow/)+ 协议机制(cross-check A/B/C/D 模板 + 12-key audit frontmatter + 4 类 DRIFT taxonomy + writeback 协议 + Lean Apply Mode + skill cascade check + budget tracker fence + runtime enforcement protocol version + dispatch matrix + autonomy boundary 6 fence)+ backlog 守门 fence(只砍 fence,**openspec/backlog/ 目录保留作信息容器**;5 archived tombstones audit trail 保留;13 active workflow-protocol + 6 capability-boundary + 9 TBD pointer 自然演化)。CLAUDE.md / AGENTS.md / README.md / docs/ai_workflow/README.md 4 docs 三大段精简(CLAUDE.md ~200 行 ForgeUE 协议段精简到 ≤30 行新 `## 工作流` section,含 OpenSpec 用法 / Superpowers 流程参考 / Codex CLI Convention / Backlog pointer 4 sub-sections)+ 加 codex CLI strong convention(`/codex:adversarial-review` design hook + `/codex:review --base main` final hook 改 opt-in via convention,preserve audit ~30-40% latent design smell catch leverage)。
- **保留**:`.claude/skills/subagent-driven-discipline/`(D11 generic universal SKILL — round 1 codex P1-1 partial-dispute → accepted-claude;sister to upstream `superpowers:subagent-driven-development`,可独立给任何 superpowers 用户使用);archived 24 changes evidence 不动(沿 D-ArchivedReplayCompat);src/framework/* ForgeUE 业务运行时不动;docs/{requirements,design,testing,acceptance}/ 五件套不动(test_spec.md 加 Level 2 ComfyUI 真机验证章节例外,沿 round 1 codex P1-5 升必做)。
- **新工作流**:OpenSpec(`/opsx:propose` + `/opsx:apply` + `/opsx:archive`)+ Superpowers 全套(brainstorming / writing-plans / subagent-driven-development / executing-plans / requesting-code-review / verification-before-completion / systematic-debugging / finishing-a-development-branch)+ codex CLI opt-in via convention 三层精简栈;不再有 ForgeUE-specific wrapper / 9 命令 / fence 守门。openspec/specs/examples-and-acceptance/spec.md 31 REMOVED + 1 MODIFIED(Centralized follow-on backlog registry minimal schema);openspec/specs/probe-and-validation/spec.md 1 REMOVED(forgeue_verify.py Level 2)+ 1 ADDED(Level 2 ComfyUI verification tool-agnostic dispatch contract,`comfy/local*` 虚拟模型 id + 禁止 `--comfy-url` flag + 禁止 LiteLLM wildcard fallback)。
- **量级**:LOC delete 实测 ~20687(对比 design.md 估计 ~9500 实际 2.18x 超估,主因 P2 grep-driven cleanup 单 commit 删 18024 LOC)。3 round codex adversarial review 6 finding 全 inline writeback disputed_open=0(5 accepted-codex + 1 accepted-claude D11 SKILL keep partial-dispute)。pytest baseline 1734 → 1136 passed / 0 failed(2 pre-existing fail 在 P2 retire fence test files 后自动消失)。
- **Sunk cost 显式 accept**(D10):`centralize-followon-backlog-registry`(2026-05-07 ship,3 天前)backlog registry fence machinery 整删 + information schema 保留;`enforce-subagent-discipline-cascade`(2026-05-08 ship,2 天前)ForgeUE-specific cascade enforcement 协议在 finish_gate retire 后自然消失,28-subtype × model tier × cheap-model reliability playbook 内容由 generic SKILL 自身承载继续 available。

### Added

- ForgeUE: `/forgeue:change-apply-subagent` Preflight Skill Cascade declared dependency 协议化加入 `subagent-driven-discipline` companion skill;Steps 第 8 step 加 model tier sub-step + quick reference table(沿 discipline §1 28-subtype model tier 表);evidence frontmatter `skill_cascade_audit.invoked_skills` template 加 `subagent-driven-discipline`。Fence test 3 case(`tests/unit/test_forgeue_command_markdown.py`)防回归:section-aware cascade(`--invoked` shell block + frontmatter YAML block-list 各自独立断言)+ model tier reference(`| implementer` / `| spec_reviewer` / `| code_quality` 表格 row 严格断言)+ direct path negative assertion(NG2 boundary)。沿 codex round 1 F1 + round 2 F1 [high] + F2 [high] 全 accepted-codex 修订。**[2026-05-11 superseded by `retire-forgeue-protocol-layer-fully`:整 retire ForgeUE-specific cascade enforcement 协议;sister skill `subagent-driven-discipline` 保留作 generic universal SKILL]**

- **F-C framework D12 video 路径分流 + F-D Evidence skip_reason field + run_import.py 精确 filter**(2026-05-08,OpenSpec change `fix-export-d12-and-skipped-evidence-filter`):cluster 2 follow-on(`fix-video-export-path-split-d12-violation` + `fix-run-import-skipped-filter-permission-only`)合并 single change 处理(通过 `evidence.json` + `manifest.json` 接口耦合;F-C framework 端写 + F-D UE 端读)。
  - **F-C framework D12 path split**(`src/framework/runtime/executors/export.py` + `src/framework/ue_bridge/manifest_builder.py`):新增 2 public helper `is_manifest_importable(art) -> bool`(`_KIND_MAP` 单一真源,消除原 modality whitelist + shape map 双源)+ `derive_drop_target(art, *, target, run_id) -> tuple[Path, str]`(video → `Content/Movies/<run_id>/MS_<base>.mp4` D12 final 位置;非 video → `Content/Generated/<run_id>/<raw_basename>` 保 raw filename 守 NG1)。`ExportExecutor.execute` drop loop 改用 `derive_drop_target`;`build_manifest` 同 helper 计算 source_uri 单源契约。`_KIND_MAP` miss(unsupported shape 如 video.webm)→ defensive fall-through 不 raise,export 不 crash(沿 round 1 codex F1 + design D10)。
  - **F-C Evidence schema**(`src/framework/core/ue.py`):`Evidence` 加 `skip_reason: Literal["permission_denied", "no_handler"] | None = None` 字段(default None 后向兼容)。`ExportExecutor` permission denied evidence emit 时显式 `skip_reason="permission_denied"`。
  - **F-D run_import.py + evidence_writer + domain_video**(`ue_scripts/`):`run_import.py` pre-scan filter 改三 AND `status=='skipped' AND skip_reason=='permission_denied' AND op_id`(消除原 skipped 单条件 over-broad 过滤);no-handler emit 时带 `skip_reason='no_handler'`。`evidence_writer.make_record` 加 optional `skip_reason` kwarg。`domain_video.import_video_entry` rewrite:删 `shutil.copy2` + 删 `mkdir`(framework 已 drop 到 D12 final);加 source_uri D12 layout 校验 + source_uri vs target_object_path mismatch fence + `file_path` 从 source_uri 派生(消除"验证一个 path / 引用另一个 path"latent design smell;沿 round 1 codex F3 + design D6 修订)。
  - **测试矩阵新增 27 case + 4 P4 integration case + 1 既有 rewrite**(详见 `docs/testing/test_spec.md` v1.6 row);全套 pytest 1700 → 1727 PASS + 3 skipped + 1 pre-existing fail(`fix-cross-check-format-test-enum-extension` follow-on,**非本 change 引入**)。
  - **3 round codex adversarial review 全 close**:S2 design round 1 4 finding(F1+F2+F4 inline writeback design D1+D2+D6+新 D10 + spec MODIFIED;F3 揭示 latent design smell)+ S3 plan round 2 2 finding(F1 P4 真机 evidence optional → 必需 + F2 tasks.md 漏 sync 2 integration case)+ S6 finalize round 3 2 finding(F1 evidence target_object_path POSIX 统一 `.as_posix()` + F2 5 follow-on 落 active.md)全 accepted-codex inline writeback,disputed_open=0。
  - **L2 live smoke + P4 real UE commandlet evidence**:run `cluster2_l2_video_export_183902`(Wan 2.1 1.3B 5sec)实证 framework drop D12 端到端;UE 5.7.4 commandlet(controller-direct,不依赖 user manual)实证 `domain_video.import_video_entry` 真机执行 `.uasset` 真实创建 + 不 copy mp4 + file_path 派生路径正确。
  - **subagent-driven-development 完整 dispatch path**:Phase A + B + C.1 各 implementer + spec_review + code_quality_review subagent + final reviewer;ADR-009 informational budget tracking($3.21 累计 advisory)。
  - **2 follow-on cancelled-completed**(active.md → archived.md tombstone):`fix-video-export-path-split-d12-violation`(F-C 修)+ `fix-run-import-skipped-filter-permission-only`(F-D 修)。
  - **5 new follow-on**(active.md workflow-protocol):S5/S6 codex review 暴露 4 finish_gate edge cases(`fix-finish-gate-completed-cancel-uses-baseline-entries` / `fix-finish-gate-followon-regex-allow-tbd-uppercase` / `fix-finish-gate-tombstone-empty-cancel-tag-bypass` / `fix-finish-gate-archived-md-protected-field-deletion`)+ 1 enum_cross_ref_check Windows GBK print(`fix-enum-cross-ref-check-windows-gbk-print`)。


- **集中 follow-on backlog registry + `_check_followon_continuity` blocker fence + `_check_srs_registry_consistency` blocker fence**(2026-05-07,OpenSpec change `centralize-followon-backlog-registry`):补"ForgeUE 当前没有集中 follow-on 记录位置"的 systemic gap。本会话 user 提问"还有哪些 follow-on"时 Claude 第一轮全项目 grep 漏 6 项实证链断 risk(`enhance-workflow-automation-handoff-persistence` / `add-forgeue-brainstorm-stage` / `enhance-workflow-automation-finishing-branch` / `enhance-workflow-automation-final-review-fence-strictness` / `enhance-workflow-automation-v2-fence-hardening` / `analyze-superpowers-skills-openspec-integration-gaps`)。
  - **新增** `openspec/backlog/active.md`(23 entries:8 workflow-protocol + 9 requirements-tbd-pointer + 6 capability-boundary)+ `openspec/backlog/archived.md`(3 first-batch tombstones,append-only 4-field schema)+ `openspec/backlog/README.md`(协议说明 + dual-source 关系 + cancel 协议 + fence enforcement)
  - **新增** `tools/forgeue_finish_gate.py` 2 blocker fence:`_check_followon_continuity`(active.md self-diff 主源 + archived tasks.md 兜底 + cancel ref strict validation + tombstone 5-point consistency + archived.md append-only)+ `_check_srs_registry_consistency`(SRS §7.3 ↔ active.md `requirements-tbd-pointer` 集合等价 + 状态变化同步)
  - **新增** 14 helpers in `forgeue_finish_gate.py`(`_extract_followon_tracking_section` / `_find_latest_archived_change` / `_parse_registry_md` / `_parse_archived_md` / `_parse_srs_tbd_table` / `_parse_tbd_pointer_entries` / `_get_change_baseline_commit` / `_get_active_md_at_commit` / `_diff_registry_entries` / `_validate_tombstone_consistency` / `_check_archived_tasks_fallback` / `_validate_cancel_tag_{superseded,not_applicable,completed}` / `_validate_cancel_refs` / `_check_archived_md_append_only`),共 ~700 LOC,~80 unit tests + 24 integration tests in new `tests/unit/test_followon_registry.py`
  - **新增** `tools/forgeue_change_state.py` 子命令 `--list-followon-{inherited,cancelled}` + 2 helpers(`list_followon_inherited` / `list_followon_cancelled`)+ 8 tests
  - **新增** evidence frontmatter 13th conditional 字段 `followon_continuity` 4-list 结构(archive-stage required;non-archive evidence 可空)
  - **5 cancel reason enum**(round 1 F2 inline writeback):`retire-superseded` / `out-of-scope` / `scope-changed` / `obsolete` / `infeasible`
  - **commit-touches strict + evidence escape hatch**(round 2 F3-r2 inline writeback):cancelled-completed tag 扩展支持 `[<commit-ref> evidence: <path>]`,fence 校验 commit 触达 source/contract_refs OR evidence path Path.exists
  - **Tombstone 5-point consistency**(round 2 F2-r2 inline writeback):id 匹配 + JSON 8-field schema + critical fields(category/source)与 baseline 一致 + archived_in_change == current change + cancellation_reason 前缀 == tasks_cancel_tag.type
  - **3 round codex adversarial review**(plan stage):round 1 design 4 finding + round 2 design 3 finding + round 3 plan 3 finding 全 accepted-codex inline writeback;disputed_open=0 across all rounds;commit chain `125eae1` → `5084166` → `c75924e`
  - **subagent-driven-development 完整 dispatch path**:P2.a-P2.h + P3 共 9 phase 通过 superpowers:subagent-driven-development skill 派 implementer + spec_review + code_quality_review + final reviewer subagent;28 evidence files in `execution/task_*_*.md` + `review/subagent_final_review.md`;ADR-009 informational budget tracking
  - **P5 dogfood**:实测 fence 检测到 2 real bugs(`_check_archived_md_append_only` GBK decode crash + SRS-registry cross-document drift 漏 sync TBD-009/TBD-013)— 全 inline fix in commit `646989c`(SRS §7.3 加 TBD-009 ✅ marker + TBD-013 row 同步 acceptance_report);protocol design 自我验证(本就是 catch 这些 cross-document drift 而设)
  - **dogfood 暴露 1 follow-on**:`fix-cross-check-format-test-enum-extension`(retire P5 verify_report.md L72 标过但未进 P12 tracking;P0.1 baseline pytest 直接命中)— 已 backfill active.md P1.3.8 验证协议有效
  - **`/forgeue:change-finish` Preflight 加 fence 描述** + `/forgeue:change-status` Output Format `### Followon Backlog` block + Steps 加 `--list-followon-*` 调用 + `change-apply-{subagent,direct}.md` evidence frontmatter template 加 `followon_continuity` 字段
  - **`docs/requirements/SRS.md` §7.3** cross-link header note 指向 `openspec/backlog/active.md` + 标识 `_check_srs_registry_consistency` fence 守门 + 加 TBD-009 ✅ + TBD-013 row 同步 acceptance_report
  - **测试矩阵新增 ~110 case**:`tests/unit/test_forgeue_finish_gate.py` 106 → 183(+77)+ `tests/unit/test_forgeue_change_state.py` 41 → 49(+8)+ `tests/unit/test_followon_registry.py` 0 → 24(new file)
  - **15 D-decision**(8 设计阶段 + 4 codex round 1 inline + 3 codex round 2 inline + 3 codex round 3 inline + parser body-boundary fix dogfood):`RegistrySchema` / `RegistryDualSource` / `FenceStrictness`(round 1 F2 + round 2 F3-r2 reverse)/ `FenceLocation` / `BackfillScope`(P0.1 dogfood 加 1 → 23)/ `EvidenceFrontmatterField`(round 1 F4 unify)/ `FenceParseStrategy`(round 1 F1 + round 2 F1-r2 reverse)/ `CrossLinkSync`(round 1 F3 fence enforce 升级)/ `TombstoneProtocol`(round 1 F1 衍生 + round 2 F2-r2 5-point consistency)
  - **2 codex round 全 close + 1 round 3 plan close**:disputed_open=0;total 10 finding all inline writeback,no disputed-permanent-drift in design / plan stages

### Removed / Retired
- **Retire ADR-011 + ADR-012 + ADR-013 + ledger-binding 全部 ForgeUE-level worktree / parallel dispatch / dispatch ledger / sister skill 强制层**(2026-05-06,OpenSpec change `retire-parallel-and-worktree-fully`;wide retire B option;user 拍板:"不再支持 subagent 并行处理任务,在这个阶段也不要支持 worktree,将 worktree 的功能和 superpowers 保持一致"):
  - **删除工具**(7 文件,~5066 LOC delete):`tools/forgeue_preflight_wrapper.py`(W1 wrapper,615 LOC)+ `tools/forgeue_dispatch_ledger.py`(W3 ledger CLI,353 LOC)+ `tools/_forgeue_ledger_crypto.py`(crypto helper,507 LOC)+ `.claude/commands/forgeue/change-apply-parallel.md`(parallel 命令模板,433 LOC)+ `tests/unit/test_dispatch_ledger.py`(1021 LOC)+ `tests/unit/test_preflight_wrapper.py`(902 LOC)+ `tests/integration/test_v2_e2e_synthetic_change.py`(1235 LOC,16 v2/v3 e2e tests);命令矩阵 10 → 9;ForgeUE stdlib-only 工具数 11 → 8。
  - **`tools/forgeue_finish_gate.py` 简化**(2820 → 1787 LOC,-1033):删 7 worktree/ledger fence 函数 + 2 helper(`_runtime_enforcement_v2_active` / `_runtime_enforcement_v3_active`)+ 5+ 常量(`_AUDIT_CONSISTENCY_MAP` / `_WORKTREE_REQUIRED_COMMANDS` / `_VALID_WORKTREE_CONSENT_OUTCOMES` / `_VALID_WORKTREE_MODES` / `_OUTCOME_MODE_INVARIANTS` 等);加 `_is_archived_replay_path` helper(沿 D-ActiveVsArchivedReplayBoundary 物理路径分支:archived 路径 + 任何 unknown value → legacy pass-through;active 路径 + present-but-invalid value → BLOCKER `unknown_protocol_version`,防 controller typo silent bypass)。
  - **`tools/forgeue_change_state.py` 简化**:删 `detect_drift_archived_replay_path`(5th DRIFT type 退回 4 类)。
  - **Sister skill `subagent-driven-discipline` partial retire**(D-SisterSkillRewrite,沿 user push back 修正前判断"功能和 worktree 还有 parallel 没关系吧"):**保留 directory + 主体内容**(§1 scenario taxonomy / §2 cheap-model reliability / §3-main / §4 / §5 historical / §6-§9 全 retire-无关基础设施);**删** §3.4.2 Type 2 Parallel Dispatch + §3.5 Worktree Consent Policy + trigger matrix Type 2 row(748 → 682 LOC,-66);v2.3 → v2.4。
  - **Backbone skill `forgeue-integrated-change-workflow` 整改**(363 → 229 LOC,-134;沿 D-BackboneSkillRewrite codex round 1 F1 finding):整删 ADR-011/012/013 sections + ADR-013 D-decision narrative;加 v1 Advisory Runtime Fence section + retire 历史 lineage。
  - **命令模板编辑**:`change-apply-subagent.md`(316 → 194 LOC)删 Preflight Worktree + Preflight Subagent Discipline + Step 10a ledger append + v3 frontmatter 字段;`change-apply-direct.md` 删 Preflight Worktree mention;`change-apply.md` 加 parallel retire 通告。
  - **测试更新**:`test_forgeue_finish_gate.py`(3844 → 2392 LOC,-1452)删 module-level `_forgeue_ledger_crypto` import + 70+ retire fence tests;`test_forgeue_command_markdown.py`(697 → 272 LOC)删 17 retire-related tests + fixture 10 → 9;`test_forgeue_workflow_no_paid_default.py` + `test_forgeue_workflow_plugin_invocation.py` fixture 10 → 9 + expected set 移除 parallel。
  - **Active 工作流退回 ADR-010 baseline**(autonomy boundary 6 fence + 12-key audit frontmatter + 4 类 DRIFT taxonomy + Documentation Sync Gate + S0-S9 状态机)+ v1 advisory 3 fence(`_check_skill_cascade` / `_check_round_fix_continuity` / `_check_task_granularity`);worktree 沿 Superpowers upstream `using-git-worktrees` SKILL OPTIONAL invoke + 自家 Step 0 consent gate;**无 ForgeUE-level 强制层**。
  - **15 D-decision**(11 设计阶段 + 4 codex round 1 inline writeback):`HardRetireScope` / `ArchivedReplayCompat` / `V1ProtocolBoundary` / `ADRRetireMatrix` / `DocResidueSweep` / `TestRemovalScope` / `MemoryEntryHandling` / `SisterSkillRewrite`(原 SisterSkillRemoval + user push back 修正)/ `PostRetireParallelStrategy` / `CommandFileRemovalVsDeprecation` / `CapabilityDeltaScope` + codex round 1 加 4:`BackboneSkillRewrite`(F1)/ archived id 格式 + 日期(F2)/ `ActiveVsArchivedReplayBoundary`(F3)/ wrapper 测试文件名(F4)。
  - **codex P5 verification review**:job `review-mou32cf3-nofa0x`,verdict `needs-attention` 4 finding(2 in-retire-scope inline writeback fix:`unresolved-permanent-drift` → `disputed-permanent-drift` + YAML flow → block-list;2 out-of-retire-scope follow-on backlog:`fix-video-export-path-split-d12-violation` + `fix-run-import-skipped-filter-permission-only`)。`disputed_open: 0`。
  - **Archived 4 change replay 兼容**(沿 D-ArchivedReplayCompat):archived `runtime-enforcement` / `executable-enforcement` / `restore-consent-gate` / `ledger-binding` evidence **不动**;P5 verify 实测 31 → 29 blocker(2 v2 fence blocker `round_fix_continuity_v2_violation` + `dispatch_ledger_violation` 在 retire 后消失,匹配 design.md 期望)。
  - **2 follow-on backlog**(本 change scope 外,P0 baseline 暴露):`fix-finish-gate-section-regex-for-p-prefixed`(`_SECTION_HEADING_RE` regex 不匹配 `## P<N>` 格式)+ `fix-openspec-validate-archived-change-support`(openspec CLI 不识别 archived change id)。

### Added
- **v3 Cryptographic Ledger Binding**(2026-05-06,OpenSpec change `enhance-workflow-automation-ledger-binding`;**superseded by `retire-parallel-and-worktree-fully` 2026-05-06**;closes archived `enhance-workflow-automation-executable-enforcement` F2+F3 round 1 codex deferred + P12.8 v2-fence-hardening superseded):
  - **新工具 `tools/_forgeue_ledger_crypto.py`**(stdlib-only,~400 LOC,internal `_*` 前缀;ForgeUE stdlib-only 工具数 10 → 11;7 函数:`canonical_payload` / `compute_hmac` / `compute_key_id` / `load_or_init_key` lifecycle 6 状态 / `verify_chain_v3` / `verify_terminal_proof` / `verify_strict_schema_v3`):HMAC-SHA256 hash chain over canonical JSON(排除 hmac + 含 prev_hmac + sort_keys + UTF-8);HMAC key 持久化到 `~/.claude/forgeue_ledger_key`(JSON 单文件,跨 change 共享,0o600 权限,跨 platform 解析 `Path.home()`);key file lifecycle 6 状态(首次 init / 正常 load / 文件损坏 exit 7 / key_id mismatch active default fail-closed BLOCKER / archived replay opt-in WARN exit 6 / forge 同 ledger 内 key_id 不一致 BLOCKER)。
  - **`tools/forgeue_dispatch_ledger.py` v3 升级**:`WRAPPER_VERSION` 1.0 → 2.0(D-WrapperVersionBump);`cmd_append` 写 11 字段 v3 schema(原 7 字段 + `protocol_version: "v3"` + `key_id`(SHA256[:16] fingerprint)+ `prev_hmac`(64 hex chars)+ `hmac`(HMAC-SHA256 over canonical));`cmd_append` stdout 打印 `[LEDGER] line_count=<N> final_hmac=<hex>` 行(D-LedgerTerminalProof,LLM 复制到 evidence frontmatter);`cmd_verify` 沿 ANY v3 信号 dispatch(任何行含 hmac/prev_hmac/key_id 字段 OR wrapper_version=2.0 OR protocol_version=v3 → trigger v3 strict;round 3 codex F1 inline writeback 防 LLM 改所有行 protocol_version 降级 v2 path);加 `--allow-archived-replay` flag(D-ArchivedReplayPathBoundary,仅 archive/ segment ledger 路径 honor flag);exit codes 0 / 5 verify_fail / 6 key_rotation_user_override / 7 key_file_corrupted。
  - **`tools/forgeue_finish_gate.py` v3 fence**:加 `_RUNTIME_ENFORCEMENT_VERSION_VALUE_V3 = "v3"` + `_VALID_PROTOCOL_VERSIONS = frozenset({v1, v2, v3})` + `_AUDIT_CONSISTENCY_MAP` module 常量 + `_runtime_enforcement_v3_active(frontmatter)` helper;**4 新 fence**:`_check_runtime_enforcement_protocol_version_validity`(round 2 codex F2 inline writeback;unknown protocol value `v4`/typo/empty BLOCKER `unknown_protocol_version`)+ `_check_archived_replay_path_boundary`(round 2 codex F1 inline writeback;active path + `ledger_archived_replay: true` BLOCKER `archived_replay_path_violation`)+ `_check_ledger_terminal_proof`(round 1 codex F3 inline writeback;v3 evidence MUST 含 `ledger_line_count` + `ledger_final_hmac` 字段 + cross-check 实际 ledger 防 tail truncation)+ `_check_ledger_forgery_resistance_consistency`(round 1 codex F4 inline writeback;v3 ↔ cryptographic / v2 ↔ advisory 强 enum)。`_check_dispatch_ledger` 加 v3 分支(import `_forgeue_ledger_crypto` + `verify_strict_schema_v3` + `verify_chain_v3` 整链 verify + key rotation 双路径)。**`_runtime_enforcement_active` 升级**(round 4 codex `/codex:review --base main` P1 inline writeback):`version in (v1, v2)` → `version in _VALID_PROTOCOL_VERSIONS` 让 v3 evidence 继承 v1 fence(`skill_cascade` / `task_granularity` / `worktree_path` / `worktree_consent_outcome` / `worktree_mode_consistency` / `parallel_decline_fallback` 7 个 v1 fence)。
  - **`tools/forgeue_change_state.py` 升级**:加第 5 类 named DRIFT detector `detect_drift_archived_replay_path`(round 3 codex F3 inline writeback;active change(非 archive/)evidence 含 `ledger_archived_replay: true` = drift signal 早期检测,与 finish_gate fence 双重守门)。
  - **15 D-decision**(round 1+2+3 codex inline writeback 后):`Scope-F3Only` / `KeyLocation` / `ProtocolVersion` / `HashChain` / `CanonicalJSON` / `KeyRotationHandling` / `FenceDispatchMatrix` / `SelfDogfoodGap` / `DispatchPath` / `WrapperVersionBump` + round 1 加 3:`LedgerTerminalProof`(F3) / `FrontmatterAuditConsistency`(F4) / `Scope-F3-MergeWithP12.8`(F5)+ round 2 加 2:`ArchivedReplayPathBoundary`(F1) / `RuntimeEnforcementProtocolVersionValidity`(F2)。
  - **测试矩阵新增 ~50 case**:`tests/unit/test_dispatch_ledger.py`(28 → 47 case;P1 16 + P2 19 + 现有 v2 12)+ `tests/unit/test_forgeue_finish_gate.py`(138 → 168 case;P3 26 v3 fence + P5 4 v3 inherits v1 fence regression)+ `tests/integration/test_v2_e2e_synthetic_change.py` 加 `TestV3CryptographicLedger` 3 case(happy + tail truncation negative + audit inconsistency negative);全套 pytest regression 1689 → 1743 PASS + 1 skipped + 0 failed。
  - **2 命令模板升级**(`.claude/commands/forgeue/change-apply-{subagent,parallel}.md`):evidence frontmatter 字段 v2 → v3(`runtime_enforcement_protocol_version: v3` / `ledger_forgery_resistance: cryptographic` / `ledger_line_count: <int>` / `ledger_final_hmac: <64-hex>` 必填 v3);Step 10a 加"读 wrapper stdout `[LEDGER]` 行 + 复制到 evidence frontmatter `ledger_line_count` / `ledger_final_hmac` 字段"明确指令(round 1 codex F3 inline writeback);加 main session serial append invariant(round 3 codex F4 inline writeback;并发 append race 由命令模板主 session 串行 append 提供,本 change scope 内不实施 cross-platform file lock,留 follow-on `enhance-workflow-automation-ledger-append-lock`)。
  - **superseded follow-on**:archived `executable-enforcement` P12.8 `enhance-workflow-automation-v2-fence-hardening`(本 change round 1 codex F5 scope expansion 已合并 schema strict validation;P12.8 follow-on 标 cancelled);P9.6 `enhance-workflow-automation-archived-replay-audit`(round 2 codex F1 inline writeback 已实施,P9.6 RETIRED)。
  - **3 new follow-on tracked**:`enhance-workflow-automation-skill-tool-binding`(F2 wrapper-bound dispatch deferred,需 hook system 改动)+ `enhance-workflow-automation-os-keystore`(R1 mitigation 用 OS-level secret store 防 LLM 主动恶意 forge)+ `enhance-workflow-automation-ledger-append-lock`(round 3 codex F4 deferred;cross-platform file lock 若实证非 ForgeUE 工作流外部并发跑 wrapper 触发 race)。
  - **threat model 边界**:本 change catch unintentional controller drift / sloppy bypass / hand-edit ledger / 中间删行 / 删尾行 attack;**不**承担"防 LLM 主动恶意 forge"(LLM 可读所有文件包括 `~/.claude/forgeue_ledger_key`)— 那是 OS-level sandbox 边界,留 future `enhance-workflow-automation-os-keystore` follow-on。

- **Enum cross-reference fence**(2026-05-06,micro-bugfix;closes deferred TODO at `tools/forgeue_finish_gate.py` L199 NOTE):
  - **新工具 `tools/forgeue_enum_cross_ref_check.py`**(stdlib-only,~340 LOC;ForgeUE stdlib-only 工具数 9 → 10):AST 扫 `tools/*.py` 抽 `_VALID_*` / `*_VALUES` / `*_TYPES` / `*_COMMANDS` frozenset 字面声明作 canonical source of truth;regex 扫 2 份 live contract doc(`CLAUDE.md` + `docs/ai_workflow/forgeue_integrated_ai_workflow.md`)抓 `<name> ∈ {…}` 描述,与 mapped canonical 做 set-equality diff;不一致 → exit 2 [DRIFT] blocker。显式映射表 `_ENUM_MAPPING` 注册 5 个核心 enum(`autonomy_decision` / `triggered_by_command` / `task_granularity` / `worktree_consent_outcome` / `worktree_mode`),其余 frozenset → advisory only(默认抑制,`--show-all` 诊断时打开)。negative lookahead `(?!_[a-z])` regex 排除 `{a,b}_suffix` 文字简写形态。
  - **40 unit fence test**(`tests/unit/test_forgeue_enum_cross_ref_check.py`):baseline regression(真实 repo 0 drift 守门)+ AST 提取(annotated / plain / 4 命名后缀 / 跳过下划线起手 / 跳过空 frozenset / 跳过非字符串字面)+ doc regex(基本 / 反引号 / 引号 / 无空格 / 排除简写)+ `_parse_doc_values` parametrize(8 形态)+ `_compute_drift`(set equality 顺序无关 / missing-in-doc / extra-in-doc / 多 occurrence 独立比对)+ E2E synthetic clean / drift + CLI exit code 协议(0 / 2 / 1)+ `--show-all` flag。
  - **wiring** `/forgeue:change-doc-sync` 命令模板新增 Step 4b(Documentation Sync Gate 内 enum cross-ref 静态扫描;exit 2 阻断 S8;由用户拍板 source-of-truth 修哪一边)+ Output Format 新增 enum cross-ref 行 + Guardrails 新增 "必跑 enum_cross_ref_check"。
  - **closes**:`tools/forgeue_finish_gate.py` L199 deferred NOTE("本 enum 与 spec.md … 必须同步;currently 无 fence test 强 cross-ref … deferred 至 P2+ enum cross-reference fence change 落地")— 替换为指向新工具的 cross-ref 守门说明。

### Changed
- **Restore Superpowers Worktree Consent Gate(2026-05-06,OpenSpec change `restore-superpowers-worktree-consent-gate`)**:
  - **ADR-013**(`docs/requirements/SRS.md` + `docs/acceptance/acceptance_report.md` ADR table):revert ADR-011 D-WorktreeEnforce + ADR-012 D-W1-ReceiptSchema mandatory worktree 部分,restore Superpowers upstream `using-git-worktrees` SKILL Step 0 user-consent gate(default decline → main repo cwd;opt-in for bug-fix iteration)。7 D-decision:D-RestoreConsentGate(命令模板 OPT-IN narrative;MUST invoke Skill 沿 upstream cascade)+ D-ConsentOutcomeStateMachine(codex round 1 F2+F3 writeback:`worktree_consent_outcome` enum + `worktree_mode` enum 必填;outcome × mode 状态机)+ D-AlreadyIsolatedInvariant(codex round 2 F2 writeback:`already_isolated` 必须 mode ∈ {skill,wrapper}_worktree + `worktree_path != main_repo`)+ D-ParallelDeclineFallback(codex round 1 F1 writeback:`/forgeue:change-apply-parallel` `declined + in_place` → 自动降级 sequential)+ D-WrapperDeprecate(`tools/forgeue_preflight_wrapper.py` 标 deprecated 但 functional)+ D-WrapperBugFixInScope(codex round 2 F3 writeback:W7-a wrapper bug fix 拨入本 change scope)+ D-CrossArchiveADRSupersede(SRS ADR-011/012 cross-reference 标 superseded by ADR-013 worktree mandatory parts)
  - **2 新 fence**(`tools/forgeue_finish_gate.py`):`_check_worktree_consent_outcome`(enum + outcome × mode invariant + W6 already_isolated path != main repo)+ `_check_worktree_mode_consistency`(mode-conditional path/receipt 字段共存);既有 `_check_worktree_path` v1 + `_check_worktree_path_v2` 改 mode-conditional advisory(legacy archived evidence pass-through)
  - **W7-a wrapper bug fix**(`tools/forgeue_preflight_wrapper.py`):`_git_repo_root` 改用 `git rev-parse --git-common-dir`(parent = main repo root)替代 `--show-toplevel`,关闭从 worktree 内调用时返 worktree 自身 → nested target → "Filename too long" 链锁失败漏洞;加 2 unit fence test(`test_git_repo_root_from_inside_worktree_returns_main_repo` + `test_wrapper_reuse_path_works_when_invoked_from_existing_worktree`)守门 regression。沿 archived ADR-012 follow-on `enhance-workflow-automation-v2-fence-hardening` P12.8 该项拨入本 change scope
  - **2 命令模板更新**(`change-apply-subagent.md` + `change-apply-parallel.md`):`## Preflight Worktree` section 重写 OPT-IN narrative(MUST invoke `Skill(superpowers:using-git-worktrees)` + outcome × mode 决策表 + decline-default narrative + opt-in wrapper_worktree 路径);parallel 命令加新 `## Preflight Parallel Decline Auto-Fallback` section。`change-apply-direct.md` 不动(沿 D-DirectWorktreeRefinement)
  - **Sister skill `subagent-driven-discipline` v2.3 update**:加新 §3.5 Worktree Consent Policy(完整 outcome × mode 决策表 + invariants + use case dispatch heuristic + wrapper deprecation note)+ Pattern 2 §3.1 STRICT cwd verify rewrite("when worktree IS used")+ Case 1 P3 worktree leak ADR-013 scope-down note + Case 3 P0+P1 retrospect(13 inline fix;6 issues;4 lessons:Pattern A reinforced + Pattern B 新 sister-file fence test sync drift + Pattern C 新 fence design intent docstring gap + Pattern D 新 controller inline fix > round 2 dispatch threshold)+ §6 catalog 加 2 row + frontmatter v2.2 → v2.3 + worktree_consent_policy + consent_outcome_enum + consent_mode_enum + case_study_count 2→3
  - **Backbone skill `forgeue-integrated-change-workflow` update**:Superpowers 集成边界表 `using-git-worktrees` 行重写(consent-gated narrative)+ Runtime Enforcement Protocol(ADR-011)+ v2(ADR-012)段加 ⚠️ Superseded note + 加新 ADR-013 Restore Superpowers Worktree Consent Gate section
  - **9 文档同步**:`docs/ai_workflow/forgeue_integrated_ai_workflow.md` §C.9 + §C.7/C.8 superseded note / `docs/ai_workflow/README.md` §4.4-quater + §4.4-bis/-ter superseded note / `docs/ai_workflow/forgeue_quickstart.md` Preflight Worktree ADR-013 update / `CLAUDE.md` Runtime enforcement frontmatter 段 ADR-013 update / `README.md` ADR-013 段 / `AGENTS.md` Restore Superpowers Worktree Consent Gate 段 / `CHANGELOG.md`(本 entry)/ `docs/requirements/SRS.md` ADR-013 行 + ADR-011/012 supersede cross-reference / `docs/acceptance/acceptance_report.md` ADR-013 status 行 + ADR-011/012 supersede note
  - **legacy 兼容**:archived ADR-011/012 evidence(无 `worktree_consent_outcome` 字段)→ 全 fence pass-through(`_runtime_enforcement_active` False 模式 + 新 fence outcome 字段 absent 时直接 return [])
  - **DogfoodSelfHostMode**(沿 archived ADR-012 self-DogfoodGap 同款):本 change 实施期沿 path A literal compliance(W7-a wrapper bug fix 后 path A 重新可行);self-evidence v2 + worktree_path / worktree_receipt_path / worktree_consent_outcome: accepted / worktree_mode: wrapper_worktree;archived 后下一个 active change 才作为 first ADR-013 default decline dogfood
  - **Pre-P0 codex round 1 design review**(adversarial):3 finding 全 accepted-codex(F1 [high] parallel decline auto-fallback / F2 [high] mode disambiguation state machine / F3 [medium] MUST invoke + outcome capture)→ writeback W1+W2+W3+W4 落 design.md + proposal.md + spec.md + tasks.md
  - **Pre-P0 codex round 2 plan review**(adversarial):3 finding 全 accepted-codex(F1 [high] disguised-as-legacy bypass / F2 [high] already_isolated invariant 漏洞 / F3 [medium] wrapper functional 合同 self-contradictory)→ writeback W5+W6+W7-a 落 design.md + spec.md + tasks.md + wrapper bug fix in scope

### Fixed
- **Finish Gate archived replay 兼容**(2026-05-07,OpenSpec change `fix-finish-gate-archived-replay-compat`;closes 2 retire-parallel-and-worktree-fully P0 baseline 暴露的 follow-on):
  - **`tools/forgeue_finish_gate.py` 4 edits**(line 1390 / 1396 / 1407-1445 / 1586-1604;~30 LOC):新常量 `_SELF_STAGE_SECTION_THRESHOLD_ARCHIVED = 10`(D-PerFormatThreshold 沿 round 1 codex F2 inline writeback 新增)+ `_SECTION_HEADING_RE` 改 `r"^##\s+(P)?(\d+)(?:\.|\s+—)\s+"` 双 capture group 暴露 P-prefix 标识(D-RegexExtension round 1 修订)+ `check_tasks_unchecked` 函数体改用 `group(2)` 抽 integer + `group(1) == "P"` 决定 per-format threshold(active ≥9 / archived ≥10)+ `build_report` openspec validate 块改用 `change_dir.is_relative_to(_common.archive_dir(repo))` repo-relative + segment-precise 检测分流(D-DispatchPathDetection round 1 codex F1 修订;archive 路径 emit `openspec_validate_skipped: archive_path_unsupported_by_upstream_cli` warning + skip subprocess,active 路径 invoke as before)。
  - **closes 2 retire P0 baseline follow-on**:`fix-finish-gate-section-regex-for-p-prefixed`(F-A:`_SECTION_HEADING_RE` 不识别 archived `## P<N> — ` em-dash U+2014 格式)+ `fix-openspec-validate-archived-change-support`(F-B:openspec CLI 不识别 `openspec/changes/archive/<dated-id>/`)。
  - **archived replay 实测**:archived 5 change finish_gate replay 31 → 1 blocker(`tasks_unchecked` 25 → 0 / `openspec_validate_failed` 5 → 0;残留 1 = retire 自家 `writeback_commit_unrelated` 不在 scope)。D-ArchivedReplayCompat criterion 真正 hold。
  - **新 9 unit fence test**(`tests/unit/test_forgeue_finish_gate.py:2342-2597`):双格式识别(active / archived em-dash) + per-format threshold(active ≥9 backward-compat / archived ≥10 P9 prereq block / archived P10/P11 self-stage skip) + 假阴性边界(`## 1.5 sub-section` / `## PX —`) + archive 路径 monkeypatch invocation count(active count==1 / archive count==0,沿 round 1 codex F3 inline writeback 防 false-pass) + `is_relative_to` 检测稳定性(active 名含 `archive` 子串 + repo 父目录路径 `archive` segment 双 case 不 false-positive,沿 round 1 codex F1 高危守门)。既有 2 baseline test PASS 守门 backward-compat。全套 `tests/unit/test_forgeue_finish_gate.py` 106/106 PASS,0 regression。
  - **4 D-decision**(round 1 codex `/codex:adversarial-review` 后):`D-RegexExtension` 修订 + `D-PerFormatThreshold` 新增 + `D-OpenSpecValidateArchiveSkip` + `D-DispatchPathDetection` 修订。codex round 1 verdict `needs-attention`(3 finding 全 accepted-codex inline writeback;`disputed_open: 0`)+ codex `/codex:review --base main` round 1 verdict `needs-attention`(2 finding 全 accepted-codex 但全 out-of-scope follow-on backlog,沿 retire 同款"out-of-retire-scope follow-on"模式;`disputed_open: 0`)+ Superpowers `requesting-code-review` final reviewer verdict `approve with fixes`(2 important + 2 minor doc-only,Important 全 inline 修复)。
  - **2 follow-on backlog**(本 change scope 外,S5 codex review 暴露,均由 prior shipped change 引入):`fix-runtime-enforcement-protocol-version-explicit-null-bypass`(F1 [P1];`forgeue_finish_gate.py:843` active evidence `runtime_enforcement_protocol_version: null` 静默绕过 `unknown_protocol_version` BLOCKER,yaml null 与 absent 不可区分;由 retire-parallel-and-worktree-fully `b593b20` 引入)+ `fix-codex-review-stage-flag-strip`(F2 [P2];`.claude/commands/codex/review.md:170` `--stage` 没 strip 就 forward 给 codex companion;由 fuse-openspec-superpowers-workflow `37288fe7` + enhance-workflow-automation P2 引入)。

- **TBD-009 编号 doc drift**(2026-05-05):`docs/acceptance/acceptance_report.md` v1.2(2026-04-23)引入 TBD-009 = RemoteControl HTTP bridge 占用编号在前;`docs/requirements/SRS.md` §7.3 v1.6(2026-05-02)引入 TBD-009 = ComfyUI mesh/audio/video 后占同号造成 doc drift。**resolution**:SRS 是权威基线(docs 五件套),保留 SRS TBD-009 = ComfyUI(已 closed);acceptance_report 的 RemoteControl HTTP bridge 重新编号 TBD-013(SRS 现有最大 TBD-012 之后第一个可用号)。改动范围:acceptance_report §7 TBD 表行 + §6.1 三通道分析行 + §8 ADR-008 兜底行三处 active reference 改 TBD-009 → TBD-013;v1.2 历史 changelog entry 保留原 TBD-009 文字作 historical record。新加 v1.9 changelog entry 描述 fix。本 fix 仅文档编号 metadata 变更,不影响代码 / 测试 / 行为契约。

### Changed
- **Workflow executable enforcement layer v2**(2026-05-05,OpenSpec change `enhance-workflow-automation-executable-enforcement`):
  - **ADR-012**(`docs/requirements/SRS.md` + `docs/acceptance/acceptance_report.md` ADR table):升级 ADR-011 v1 advisory not deterministic 为 v2 executable enforcement layer — D-W1-ReceiptSchema(wrapper 自管 worktree + 13-field receipt JSON)+ D-W2-OverlapDetection(parallel actual diff git status --porcelain + ls-files --others 合集 + 自动降级)+ D-W3-LedgerFormat(JSONL append-only ledger)+ D-W3-WrapperImpl(post-dispatch capture 真实 agent_id)+ D-DispatchWrapperBoundary(LLM 不能直接写 receipt / ledger)+ D-DegradationPath(overlap 自动降级 sequential)+ D-FrontmatterSchemaExtension(protocol_version v2 + 7 v2 字段)+ D-W4-IntegrationGate(P5.5 v2 e2e fixture archive 必过 gate)+ D-DogfoodGap(本 change evidence 仍 v1 advisory)
  - **新工具 W1 + W3**(沿 D-W1-ReceiptSchema + D-W3-LedgerFormat;stdlib only):`tools/forgeue_preflight_wrapper.py`(584 LOC + 18 fence test)+ `tools/forgeue_dispatch_ledger.py`(150 LOC + 12 fence test);ForgeUE stdlib-only 工具数从 7 → 9
  - **`forgeue_finish_gate.py` v2 升级**(367 LOC + 524 LOC test;16 new fence + 4 protocol dispatch test):4 v2 fence(`_check_worktree_path_v2` receipt cross-check + `is_isolated_worktree: true` / `_check_round_fix_continuity_v2` ledger agent_id 集合 cross-check / `_check_file_overlap_actual` parallel only / `_check_dispatch_ledger` inline ledger verify);`runtime_enforcement_protocol_version` dispatch matrix(legacy / v1 / v2 三路;v2 = v1 + additional checks 严格于 v1;archived runtime-enforcement evidence 兼容)
  - **2 命令模板升级**:`change-apply-subagent.md` + `change-apply-parallel.md` Preflight Worktree section invoke `python tools/forgeue_preflight_wrapper.py`(W1)+ post-dispatch ledger append step(W3 真实 agent_id 从 Skill(Task) return capture)+ evidence frontmatter v2 字段(parallel 含 W2 actual diff Step 0/1/2 Bash 段 — git status --porcelain precondition + git diff -z + git ls-files --others -z 合集 + Bash dict→JSON 序列化 + cross-implementer set intersection + 自动降级);abort log 沿 ForgeUE 产物路径 `<change>/parallel_abort_*` 不 `/tmp/`
  - **新 sister skill `subagent-driven-discipline`**(controller-side 40% scenario judgment;v2.2 — 28 task subtypes scenario taxonomy + cheap-model reliability playbook + Trigger Type Matrix retrospect 5 types × per-type intensity;design 任务绝对原则 Opus only)+ Layer 2 wiring(`/forgeue:change-apply-{subagent,parallel}` Preflight Subagent Discipline section MANDATORY invoke skill)
  - **Pre-P0 codex round 1 design review**(5 high finding 全 accepted-codex,3 inline + 2 deferred to follow-on `enhance-workflow-automation-ledger-binding`):F1 wrapper self-managed worktree(inline)+ F4 git status --porcelain 合集 + clean precondition(inline)+ F5 P5.5 v2 e2e fixture(inline)+ F2 wrapper-bound dispatch / F3 cryptographic ledger(deferred)
  - **Round 2 codex plan review**(4 finding 全 inline plan-stage drift):execution_plan + micro_tasks 同步 round 1 inline writeback(P0 13 字段 + 18 fence + wrapper self-create / P3 post-dispatch ledger capture / W2 git status --porcelain 合集 + 不 /tmp / Phase Map 14 phase 独立 row 含 P5.5)
  - **9 文档同步 + tests/integration/test_v2_e2e_synthetic_change.py archive 必过 gate**:`docs/ai_workflow/forgeue_integrated_ai_workflow.md` §C.8 / `docs/ai_workflow/README.md` §4.4-ter / `docs/ai_workflow/forgeue_quickstart.md` v2 wrapper 摘要 / `CLAUDE.md` 工具 7→9 + v1/v2 字段 + dispatch matrix + Layer 2 wiring / `README.md` ForgeUE Workflow 表 7→9 + ADR-012 / `AGENTS.md` v2 enforcement 段 / `docs/requirements/SRS.md` ADR-012 / `docs/acceptance/acceptance_report.md` ADR-012 / `docs/design/HLD.md` workflow tooling W1/W3 wrapper

- **Workflow runtime enforcement layer**(2026-05-05,OpenSpec change `enhance-workflow-automation-runtime-enforcement`):
  - **ADR-011**(`docs/requirements/SRS.md` + `docs/acceptance/acceptance_report.md` ADR table):D-WorktreeEnforce + D-DirectWorktreeRefinement + D-SkillCascadeCheck + D-RoundFixContinuity + D-TaskGranularityDeclaration + D-ParallelDispatch + D-PreflightProtocol + D-ProtocolVersionMigration 合记 ADR — runtime enforcement layer 把 declared-only Layer 6 cascade dependency / worktree isolation / round 2 continuity / task granularity 转为 advisory enforcement(命令模板显式步骤 + LLM 自报 frontmatter declaration + finish_gate audit)
  - **新命令 `/forgeue:change-apply-parallel`**(D-ParallelDispatch):invoke Superpowers `dispatching-parallel-agents` SKILL(借用 pattern;debugging-focused → implementation 借用),controller 显式判定 task 独立(`task_independence_assertion: true` + `task_files_disjoint: [<file-set>...]`,命令前自动 verify file overlap → 任一交集 abort)后路由;Active forgeue 命令数从 9 → 10
  - **D-DirectWorktreeRefinement**(2026-05-05 user 拍板):`change-apply-direct` 沿 archived `2026-05-04-adopt-subagent-driven-development` D-Worktree-Detail 第 5 项**仍跑在主 worktree**(direct 是 < 3 micro-task 轻量 fallback,worktree 创建 ~10-20s 开销不划算);drift writeback commit `15ae851`(双向 spec.md / design.md / finish_gate fence / 测试一并落账)
  - **新工具 `tools/forgeue_skill_cascade_check.py`**(stdlib only,384 lines):静态扫 SKILL.md `## Integration` 段验证 dependency 全 invoke;8 root probe 链(CLI flag / env var / repo-local / Anthropic plugin cache 最新 version / 其他 plugin / Codex / `${CODEX_HOME}` / `.agents/skills`)
  - **新增 4 runtime fence**(`tools/forgeue_finish_gate.py`):`_check_skill_cascade` / `_check_round_fix_continuity` / `_check_task_granularity` / `_check_worktree_path`;protocol gate `runtime_enforcement_protocol_version: v1`,无此字段视为 legacy(fence pass-through;archived `enhance-workflow-automation` 等历史 change replay 兼容)
  - **8 个 SKILL-invoke 命令 Preflight section**(D-PreflightProtocol):subagent + parallel 含 3 段(Worktree + Skill Cascade + Task Granularity);direct 含 2 段(Skill Cascade + Task Granularity;沿 D-DirectWorktreeRefinement 不含 Worktree);plan/debug/verify/review/doc-sync 含 1 段(Skill Cascade);change-finish + change-status + codex /review + /adversarial-review 不含(纯工具 / 只读 / 纯 codex CLI dispatch,disclaimer 路径)
  - **新增 evidence frontmatter 字段**:`runtime_enforcement_protocol_version: v1`(协议版本标记)/ `worktree_path`(D-WorktreeEnforce + D-DirectWorktreeRefinement)/ `skill_cascade_audit` dict(`invoked_skills` list + `cascade_check_pass_at` ISO timestamp)/ `subagent_continuity` dict(round 1/2 implementer + reviewer ID)/ `task_granularity` ∈ {phase, per-file, sub-task} / `task_independence_assertion` + `task_files_disjoint`(仅 parallel)
  - **`_common.py::_parse_yaml_subset`**:扩 nested mapping 解析支持 `subagent_continuity` / `skill_cascade_audit` dict 字段(纯加性 — 仅当 raw 空 + 下一非空缩进行非 `- item` 时进 nested 分支;不破坏现有解析路径)
  - **真 deterministic enforcement 留 follow-on `enhance-workflow-automation-executable-enforcement`**:R6(advisory not deterministic limitation;codex round 1 F1/F2/F3 揭示 markdown enforcement is advisory not deterministic — controller 跳过 markdown step 时 subagent 已修改 + finish_gate 是 archive 时才扫,无法 abort)— W1 executable preflight wrapper + machine-generated receipt JSON / W2 actual changed-files diff overlap detection / W3 dispatch ledger 命令层 wrapper 写
  - **11 处文档同步**:
    - `docs/ai_workflow/forgeue_integrated_ai_workflow.md` 加 §C.7 "Runtime Enforcement Protocol"(4 fence 表 + protocol gating + 新命令 + 8 Preflight section 表 + D-DirectWorktreeRefinement)
    - `docs/ai_workflow/README.md` §4 加 §4.4-bis "Runtime Enforcement"
    - `docs/ai_workflow/forgeue_quickstart.md` S2/S3/S4-S5 加 路由决策树 + Preflight 三项摘要 + change-apply-parallel 命令引用
    - `CLAUDE.md` `## OpenSpec 工作流` 命令清单从 9 → 10(加 `/forgeue:change-apply-parallel` 行 + D-DirectWorktreeRefinement 修订 direct 行);工具清单从 6 → 7(加 `forgeue_skill_cascade_check.py`);finish_gate 描述加 4 runtime fence + protocol gate;新增 "Runtime enforcement frontmatter 字段" 段
    - `README.md` ForgeUE Workflow 表 9 → 10 命令(加 `/forgeue:change-apply-parallel` + D-DirectWorktreeRefinement direct 修订);6 → 7 工具(加 cascade check);新增 ADR-011 摘要段
    - `AGENTS.md` 加 4 条 runtime enforcement 摘要(parallel 命令 / 8 SKILL-invoke 命令 Preflight section / 4 runtime fence / cascade check 工具)
    - `CHANGELOG.md` [Unreleased] 加本 change entry(当前条目)
    - `.claude/skills/forgeue-integrated-change-workflow/SKILL.md` 加 runtime enforcement + 新命令 + 4 fence 段
    - `docs/requirements/SRS.md` 加 ADR-011 行(沿 ADR-007/008/009/010 格式)
    - `docs/acceptance/acceptance_report.md` 加 ADR-011 status 行
    - `openspec/specs/examples-and-acceptance/spec.md` 留 archive 时 openspec 自动 sync(P10 archive 协议)
  - **主要 commit SHA**:3b7728e(P0 forgeue_skill_cascade_check + 18 fence)/ 6157752(P1 4 runtime fence + 16 fence test)/ 15ae851(D-DirectWorktreeRefinement drift writeback)/ c23f638(P2 Preflight sections + change-apply-parallel + 7 fence test)/ 0dc400d(P3 codex disclaimer + 1 fence test)
  - **测试覆盖**:以 `python -m pytest -q` 实测为准(skill_cascade_check 18 fence + finish_gate 16 runtime fence + command_markdown 7 P2 fence + codex_command_markdown 1 P3 fence,全套 1529+ passed)

- **Workflow autonomy boundary + default background codex review**(2026-05-05,OpenSpec change `enhance-workflow-automation`):
  - **ADR-010**(`docs/requirements/SRS.md` + `docs/acceptance/acceptance_report.md` ADR table):D-AutonomyBoundary "Workflow autonomy boundary fence" — Claude 默认拍板 + 自动 codex 二次验证 + 6 类 fence 升级用户(不可逆 / 跨 change / Claude+Codex 冲突 / 用户约束 / 钱 / 安全)
  - **D-DefaultBackground**:`/codex:review` / `/codex:adversarial-review` 默认 background 分发;仅全部 3 条满足(≤2 files / ≤50 lines / 非 adversarial / 下一步必须等结果)才前台 wait;命令模板保留 `--wait` / `--background` 显式 flag;background 启动 job id 写 `notes/<review_type>_active_jobs.txt`;**移除 "Do not call BashOutput" 矛盾文本**,替换为 "Main session MUST poll job before consuming verdict via /codex:status --wait + /codex:result"
  - **D-CodexContextBridge**:同 `change_id` + 同 `review_type` round N→N+1 prompt 首段自动注入 round N evidence reference;round counter 落 `notes/codex_<review_type>_round_counter.txt`;round 1 无注入;跨 change / 跨 review_type 绝不共享
  - **autonomy_decision frontmatter 字段**:每条 implementation evidence 必填;`claude_codex_concurred` 必配 `codex_review_ref`;`forgeue_finish_gate.py` `_check_autonomy_boundary` fence 守门(缺字段 / 值非法 / ref 路径不存在 / ref 跨 change / ref 未 finalize → exit 非 0)
  - **9 个 forgeue 命令模板 + 2 个 codex 命令模板**加 `## Decision Delegation` section,显式声明哪些 step Claude 自主走 / 哪些必须请求用户
  - **11 处文档同步**:
    - `docs/ai_workflow/forgeue_integrated_ai_workflow.md` 加 §C "Autonomy Boundary Protocol"(D-AutonomyBoundary + 6 类 fence + autonomy_decision + D-DefaultBackground + D-CodexContextBridge + edge cases;原 §C/§D 顺延为 §D/§E)
    - `docs/ai_workflow/README.md` §4 加 §4.4 "决策权下放与 Autonomy Boundary" 摘要
    - `docs/ai_workflow/forgeue_quickstart.md` S2/S5/S6 stage 描述加 default background + autonomy_decision 字段说明
    - `CLAUDE.md` `## OpenSpec 工作流` 加 "决策权下放" 摘要(6 类 fence 列表)
    - `README.md` ForgeUE Workflow 描述加 default background + autonomy boundary 说明
    - `AGENTS.md` 加 "决策权下放与 Autonomy Boundary" 同款摘要
    - `CHANGELOG.md` [Unreleased] 加本 change entry(当前条目)
    - `.claude/skills/forgeue-integrated-change-workflow/SKILL.md` 加 autonomy boundary + codex default background 协议
    - `docs/requirements/SRS.md` 加 ADR-010 行
    - `docs/acceptance/acceptance_report.md` 加 ADR-010 status 行
    - `openspec/specs/examples-and-acceptance/spec.md` 留 archive 时 openspec 自动 sync(P3.9 confirm skip)
  - **主要 commit SHA**:99540e2 / 1ea80b5 / 730de52 / 55d15d7 / 1e4dfb9 / 8e897c4 / c6913ae / 8b1f9cc
  - **测试覆盖**:以 `python -m pytest -q` 实测为准(文档 + finish_gate fence + forgeue_command_markdown fence + codex_command_markdown fence)

- **Adopt Superpowers `subagent-driven-development` as default `/forgeue:change-apply` path**(2026-05-04,OpenSpec change `adopt-subagent-driven-development`):
  - **8 项 D 决议**(用户拍板):D-Worktree(解禁 `using-git-worktrees` REQUIRED;纯文档修改代码 0 改动)/ D-Default(`/forgeue:change-apply` 拆 `change-apply-subagent`(default)+ `change-apply-direct`(fallback);拒绝 env flag facade)/ D-EvidenceSchema(per-task 4 类 evidence 扁平命名 `task_<n>_{implementer,spec_review,code_quality_review}.md` + `subagent_final_review.md`;`triggered_by_command: change-apply-subagent` audit field 判定 dispatch mode)/ D-SkillInvoke(直接 invoke skill 不重写 prompt 模板)/ D-TaskInput(完整 task text 作 prompt 不让 subagent 读 plan files)/ D-ADR009(token-budget tracker informational + soft WARNING;与 ADR-007 vendor API 双扣边界根本不同)/ D-BudgetMode(stdout `[WARN]` + exit 0 始终)/ D-SelfHost(本 change 用 subagent-driven-development 跑 dogfooding self-host bootstrap)
  - **NEW ADR-009**(`docs/requirements/SRS.md` ADR table line 383):subagent dispatch token-budget tracker informational tracker(`tools/forgeue_subagent_budget.py` 实装;ADR-008 编号已被 acceptance_report.md A1 立项 "UE plugin" 占用,本表跳号至 ADR-009)
  - **NEW**:9 `/forgeue:change-*` commands(原 8 + `change-apply-subagent` + `change-apply-direct` - 1 deprecated `change-apply` stub)— design.md D-Default + D-Worktree-Detail 16-step subagent dispatch flow(commit untracked → invoke `using-git-worktrees` → cwd switch → invoke `subagent-driven-development` → 4 类 evidence 收口 → budget record → 越界检测 → writeback → squash merge → worktree remove)
  - **NEW**:6 `tools/forgeue_*.py`(原 5 + `forgeue_subagent_budget.py` 524 lines stdlib-only — `--status` / `--record` / `--json` 子命令 + JSON Lines append-only writer + two-tier WARN(total + per-task)+ exit 0 always)
  - **NEW evidence schema**:4 类 per-task subagent evidence_type(`subagent_implementer_report` / `subagent_spec_review` / `subagent_code_quality_review` / `subagent_final_review`)+ `triggered_by_command` audit field;`tools/forgeue_finish_gate.py` `_REQUIRED_EVIDENCE_SUBAGENT` enum + `_detect_subagent_dispatch_mode` 函数 + `_check_per_task_triple` per-task triple validation(F7 codex S6 round 2 fix)
  - **NEW DRIFT detector keywords**:`tools/forgeue_change_state.py::_KNOWN_FAILURE_KEYWORDS` 加 reviewer gap keywords(`missing requirement:` / `extra feature:` / `misunderstood:`,colon-anchored 避免 generic phrase false-positive;F8 codex S6 round 2 fix)
  - **NEW fence**:33 fence test 总(11 task 3 §5 + 22 task 4 §6)+ 2 codex S6 round 2 fix fence(F7 per-task triple + F8 reviewer gap keyword);1450 PASS / 1 SKIP / 0 ERRORS
  - **2 轮 codex review hook + dogfood self-host bootstrap**:codex round 1 5 finding(F1-F5 全 accepted-codex written-back-to-* with real `writeback_commit` shas)+ codex S6 round 2 5 finding(F6-F10 全 accepted-codex 同款)+ Layer 1-5 dogfood meta-finding 累积(cross-file collision / protocol gap / decision-level insight / scope justification rigor / codex catches final reviewer's blind spots)
  - **Dogfood cumulative cost**(manual_estimate):~$10.6(default WARN $2.0/change × 5.3,informational 不阻断)
- **ComfyUI agent CLI video capability adoption(Phase 3 of TBD-009 — TBD-009 全 phase closed)**(2026-05-04,OpenSpec change `comfy-agent-cli-video-adoption`):
  - **5 项 D-fixed 用户拍板决策**(2026-05-04):D1 UE 资产语义 = `unreal.FileMediaSource` + `.mp4`(`_KIND_MAP[("video","mp4")] = "file_media_source"` + `MS_` prefix);D2 modality Literal 只扩 `"video"` 单项(animation / cinematic 走 shape 字段细分);D3 默认 manifest = `Vedio/Wan2.1-T2V-1.3B_native_5sec`(7 分钟 / 6GB VRAM);D4 mp4 全字节读 + follow-on `repo-put-streaming-payload`(SRS §7.3 TBD-012 占位);D5 `Vedio/` 拼写照实跟随上游(改名破坏 ComfyUI 自家 workflow + custom node 索引)
  - **NEW**:`src/framework/providers/workers/video_worker.py` 新 module — `VideoCandidate(data, format, metadata, duration_seconds=None, frame_count=None, width=None, height=None, fps=None)` dataclass(`format: Literal["mp4"]` mp4-only round-2 F2 + round-3 PF3 sweep,webm follow-on `comfy-video-webm-adoption`;5 顶层 None defaults — ComfyUI agent CLI 不暴露,follow-on `video-metadata-parser` 加 ffprobe 解析填充)+ `VideoWorker(ABC)` baseline ABC + 异常树(`VideoWorkerError` / `VideoWorkerTimeout` / `VideoWorkerUnsupportedResponse`)+ `FakeVideoWorker`(`_build_minimal_mp4` 合成 32-byte BMFF strict 5-tuple-conformant ftyp box,无第三方 codec 依赖)
  - **NEW**:`ComfyAgentWorker` 4-dict capability dispatch 扩 `video` capability — `_CAPABILITY_BY_MODEL_ID["comfy/local-video"] = "video"` + `_REQUIRED_OUTPUT_KEY["video"] = "video"` + `_AUXILIARY_OUTPUT_KEYS_BY_CAP["video"] = set()` + `_REJECTED_OUTPUT_KEYS_BY_CAP["video"] = {"images", "glb", "audio"}` + `_VIDEO_FORMAT_WHITELIST = {"mp4"}` 类常量;新 public 方法 `generate_video(spec, num_candidates, seed, timeout_s) -> list[VideoCandidate]`(per-candidate loop 在 worker 内沿 audio F-Plan-3 + F-Plan-R5-A round-5 模式;subprocess.run 一次只生成一个);`_run_once_video` helper 走 subprocess.run + stdout JSON parse + `_validate_outputs` 三段表 + 路径 trust-boundary(`is_file()` + `is_symlink()`)+ 扩展名 whitelist mp4-only + **BMFF strict 5-tuple header validation**(round-2 F4 + round-3 PF2:`len >= 16` + `data[4:8] == b"ftyp"` + `box_size in [8, len(data)]` reject `box_size == 1`(largesize follow-on `video-bmff-largesize-support`)+ `data[8:12]` major_brand non-empty / non-zero / non-spaces)+ VideoCandidate 构造 + 5 `comfy_*` metadata 键
  - **NEW**:`src/framework/runtime/executors/generate_video.py` 新 module — `GenerateVideoExecutor` (`step_type = StepType.generate, capability_ref = "video.t2v"`,沿 audio R3-A 模式)— `_should_use_comfy_worker_path()` 判 `model == "comfy/local-video"` in `provider_policy.prepared_routes` 顶层;`_generate_via_comfy_worker` 走三 except 块(`ComfyWorkerTimeout → VideoWorkerTimeout` retry honor `_should_retry(policy, wrapped)` per audio F-Plan-R7-B / `ComfyWorkerUnsupportedResponse → VideoWorkerUnsupportedResponse` immediate raise / `ComfyWorkerError → VideoWorkerError` immediate raise,均 `from exc`);`execute()` 走 `repo.put` with `ArtifactType(modality="video", shape="mp4", display_name="video_asset")`(D1 + D8:`shape="mp4"` 是 UE bridge `_KIND_MAP[("video","mp4")] = "file_media_source"` 唯一映射)+ `file_suffix=f".{cand.format}"` + `metadata={"format": ..., "duration_seconds": None, "frame_count": None, "width": None, "height": None, "fps": None, "worker_metadata": dict(cand.metadata)}`
  - **NEW UE bridge video 资产链路**(D1 + D12,从零建):`src/framework/ue_bridge/manifest_builder.py` `_KIND_MAP` 加 `("video", "mp4"): "file_media_source"` + `_PREFIX_BY_KIND["file_media_source"] = "MS_"`(沿 SM_/S_/T_/M_ 风格)+ `_default_import_options` 加 video 分支(loop / play_on_open / 5 None metadata fields + source_format)+ `metadata_overrides` 白名单加 video keys(frame_count / fps / loop / play_on_open;width / height 已存在沿用 image 模式);`src/framework/ue_bridge/import_plan_builder.py::_IMPORT_OP_KIND` 加 `"file_media_source": "import_file_media_source"`;**新建** `ue_scripts/domain_video.py::import_video_entry`(`unreal.FileMediaSourceFactoryNew` + `unreal.AssetTools.create_asset` + `set_editor_property("file_path", relative_path)`;**D12 关键 packaging path 分流**:mp4 source 落 `<project_root>/Content/Movies/<run_id>/MS_<base>.mp4`(packaging 外挂 — UE 5.x packaging 把 Content/Movies/ 打包为 standalone movie file 而非 .uasset 内嵌),`.uasset` 落 `<project_root>/Content/Generated/<run_id>/MS_<base>.uasset`(asset_root 沿用与 audio / mesh / image 一致),`FileMediaSource.file_path` 字段指向 `Movies/<run_id>/MS_<base>.mp4` 相对 Content/ 路径 UE runtime 解析;NFR-PORT-003 守:domain_video 只 import unreal + stdlib,不 import framework.*);`ue_scripts/run_import.py::_OP_HANDLERS` 加 `"import_file_media_source": domain_video.import_video_entry` dispatch
  - **Round-2 F1 export gate 4 处 sweep**(round-2 codex design review 揭示的盲点 + commit 8c 实施时发现第 4 处):`src/framework/runtime/executors/export.py::_is_importable` modality whitelist 加 `"video"`(否则 video Artifact 在 ExportExecutor 阶段被静默过滤,不进 manifest_builder)+ `src/framework/core/policies.py::PermissionPolicy` 加 `allow_import_file_media_source: bool = True`(沿 image / audio / mesh import 同 default-allow tier)+ `src/framework/ue_bridge/permission_policy.py::_OP_ALLOW_ATTR` 加 `"import_file_media_source": "allow_import_file_media_source"` mapping(否则 is_op_allowed 走 unknown → deny → ExportExecutor 给 video op 发 status="skipped" Evidence)+ `src/framework/core/ue.py::UEImportOperation.kind` Pydantic Literal 加 `"import_file_media_source"`(否则 import_plan_builder 构造 op 时 Pydantic ValidationError;round-3 plan review 漏检的位置)。**4 处缺任一处都会断 video bridge 链路**,本 change commit 8c 同 commit 一次性修齐
  - **Round-3 PF1 D-Runner-Extension**(用户 2026-05-04 拍板路径 (a) 扩 ComfyUI runner.py):`D:/AI/ComfyUI/scripts/comfyui_api/runner.py::extract_outputs` 加 `video` collection block(收集 VHS_VideoCombine 节点 legacy `gifs` UI key 装的 video preview dict — 实测 `nodes.py:633` `return {"ui": {"gifs": [preview]}, "result": ...}`,preview dict 含 `filename` / `subfolder` / `type` / `format` / `frame_rate` / `workflow` / `fullpath`;fast-path 优先 `fullpath` + fallback `subfolder/filename`)。沿 Phase 1 round 5 D10 mini-LoadImage user-authored 模式 — ComfyUI 重装时**手工保留**(否则 ForgeUE video L2 evidence 失败)
  - **NEW**:`config/models.yaml` + `tests/fixtures/test_models.yaml` 加 2 entry — `models.comfy_local_video`(virtual id `comfy/local-video` + provider=comfy_api + kind=video + pricing=null)+ `aliases.video_local`(preferred=[comfy_local_video];SRS FR-MODEL-007 alias 列表第 12 项)
  - **NEW**:`runtime/failure_mode_map.py` — `FailureMode.video_worker_timeout` + `video_worker_unsupported`(均 `Decision.abort_or_fallback`,沿 audio_worker_* / mesh_worker_* 同模式;`VideoWorkerError` 归类 `video_worker_unsupported`)— `classify()` 顺序:**D14 priority** video 子类**先于** audio / mesh / generic worker_* isinstance(沿 audio R4-F1 priority 修订模式;wrapped VideoWorker* 子类必须先于通用父类匹配,否则被抢先吞掉失去 video-specific decision)
  - **NEW**:`runtime/dry_run_pass.py::_check_comfy_reachability` gate set 从 `{comfy/local, comfy/local-mesh, comfy/local-audio}` 扩为 `{comfy/local, comfy/local-mesh, comfy/local-audio, comfy/local-video}`(TBD-009 全 phase closed 4 元素 set membership)
  - **NEW**:`examples/comfy_local_smoke_video.json` text-to-video bundle(单 step `video.t2v` + `provider_policy.models_ref="video_local"` + `step.retry_policy={max_attempts:2, backoff:"fixed", retry_on:["timeout","provider_error"]}` 顶层 + `step.config.{worker_timeout_s: 600, num_candidates: 1, seed: 5042, spec.{comfy_workflow:"Vedio/Wan2.1-T2V-1.3B_native_5sec", comfy_params:{positive_prompt, negative_prompt, width:832, height:480, num_frames:81, seed:5042, steps:25}, comfy_lifecycle:"none"}}`)
  - **NEW**:`probes/provider/probe_comfy_video.py` opt-in probe — `FORGEUE_PROBE_COMFY_VIDEO=1` 才跑(默认 SKIP);本地 GPU subprocess ~7 分钟 + 首次模型权重 HuggingFace 拉 ~3GB Wan 2.1 1.3B;模块顶层零副作用(L3 fence 守门);BMFF strict 5-tuple in-probe 校验
  - **NEW fence 文件 + delta**:`tests/unit/test_artifact.py`(10 fence 含 P12 regression — `tests/unit/test_core_schemas.py::test_artifact_type_modality_enum_check` 既有 fence 用 modality="video" 占位被 commit 1 命中,改为 modality="hologram")+ `tests/unit/test_video_worker.py`(7 fence)+ `tests/unit/test_comfy_subprocess_video.py`(27 fence,含 BMFF strict 5-tuple 9 fence + DryRunPass video gate fence)+ `tests/unit/test_generate_video_comfy.py`(14 fence);delta `tests/unit/test_failure_mode_map.py`(video +6)+ `tests/unit/test_ue_bridge.py`(video manifest 7 + F1 sweep 3 = 10)+ `tests/integration/test_p4_ue_manifest_only.py`(video stub 3 + F1 sweep 2 = 5)+ `tests/unit/test_probe_framework.py`(video +2)+ `tests/unit/test_model_registry.py`(video +2)+ examples bundles auto-discover 3 fence;共 +120 fence;~1294 baseline → 1414 final pytest -q
  - **5 轮 codex review writeback**:1 design adversarial(round-2 4 finding F1-F4 全 accepted-codex 落 design.md `## Reasoning Notes — round-2 codex review`,sweep design + 5 spec deltas + tasks)+ 1 plan adversarial(round-3 4 finding PF1-PF4 全 accepted-codex 落 design.md `## Reasoning Notes — round-3 codex plan review`,含 D-Runner-Extension 用户拍板路径决策)+ 3 implementation review(round-4/5/6,2 finding RF1-RF2 全 accepted-codex sweep PF2 largesize + PF3 mp4-only 残留,round-6 verdict approve);Apply-stage 16-commit chain
  - **TBD register update**:SRS §7.3 TBD-009 全 3 phase closed(2026-05-04);新增 TBD-012 `repo-put-streaming-payload`(D4 副作用 follow-on,触发条件:第一个 ≥100MB mp4 真实 use case 出现);Follow-on 登记(不开占位 change):`comfy-video-webm-adoption`(F2 副作用)+ `video-bmff-largesize-support`(PF2 副作用)+ `video-metadata-parser`(parallel to audio-metadata-parser)+ `video-worker-remote-adoption`(远端 Runway / Pika / Sora)+ `comfy-video-image-sequence-adoption`(D1 (α) 路径)
  - **L2 evidence + a2_video UE 真机 P4 commandlet 验收**:待用户在装 UE 5.x 的本机跑(沿 a2_mesh 2026-04-23 commandlet 模式;Wan 1.3B 7 分钟 generation;evidence 落 OpenSpec change archive notes/)
  - **测试覆盖**:**pytest -q 实测 1414 passed**(prior 1294 baseline + 120 fence net delta:10 commit 1 + 7 commit 2 + 2 commit 3 + 27 commit 4 + 14 commit 5 + 6 commit 6 + 7 commit 7 + 3 commit 8 + 6 commit 8c + 1 commit 9 + 3 commit 10 + 2 commit 11 = 88 + 32 cross-cutting docs / regression / Pydantic Literal extension fence);Level 0/1 全 OK;Level 2 + a2_video 真机待用户跑
- **ComfyUI agent CLI audio capability adoption(Phase 2 of TBD-009 + TBD-002 lift)**(2026-05-03,OpenSpec change `comfy-agent-cli-audio-adoption`):
  - **NEW**:`src/framework/providers/workers/audio_worker.py` 新 module — `AudioCandidate(data, format, metadata, duration_seconds=None, sample_rate=None)` dataclass(`format: Literal["flac","mp3","wav"]` whitelist;F-Plan-R7-A:`format`/`duration_seconds`/`sample_rate` source-of-truth 在 dataclass 顶层,**不**在 `metadata` 嵌套 dict 内重复)+ `AudioWorker(ABC)` baseline ABC + 异常树(`AudioWorkerError` / `AudioWorkerTimeout` / `AudioWorkerUnsupportedResponse`)+ `FakeAudioWorker`(`_build_minimal_flac` 合成有效 FLAC bytes,~50-100 字节,无第三方 codec 依赖)
  - **NEW**:`ComfyAgentWorker` 4-dict capability dispatch 扩 `audio` capability — `_CAPABILITY_BY_MODEL_ID["comfy/local-audio"] = "audio"` + `_REQUIRED_OUTPUT_KEY["audio"] = "audio"` + `_AUXILIARY_OUTPUT_KEYS_BY_CAP["audio"] = set()` + `_REJECTED_OUTPUT_KEYS_BY_CAP["audio"] = {"images", "glb", "video"}` + `_AUDIO_FORMAT_WHITELIST = {"flac", "mp3", "wav"}` 类常量;新 public 方法 `generate_audio(spec, num_candidates, seed, timeout_s) -> list[AudioCandidate]`(F-Plan-3 / R5-A:per-candidate loop 在 worker 内,subprocess.run 一次只生成一个);`_run_once_audio` helper 走 subprocess.run + stdout JSON parse + `_validate_outputs` 三段表 + 路径 trust-boundary(`is_file()` + `is_symlink()`)+ 扩展名 whitelist + magic bytes 二次校验(`fLaC` / `ID3`+MPEG sync `0xFF 0xFB / 0xFA / 0xF3 / 0xF2` / `RIFF`+offset8`WAVE`)+ AudioCandidate 构造 + 5 `comfy_*` metadata 键(`comfy_workflow` / `comfy_run_root` / `comfy_filename_prefix` / `comfy_original_filename` / `comfy_run_id`)
  - **NEW**:`src/framework/runtime/executors/generate_audio.py` 新 module — `GenerateAudioExecutor` (`step_type = StepType.generate, capability_ref = "audio.t2a"`)— `_should_use_comfy_worker_path()` 判 `model == "comfy/local-audio"` in `provider_policy.prepared_routes`(top-level,F1 round-2 plan 修订);`_generate_via_comfy_worker` 走 F2 三-except 块(`ComfyWorkerTimeout → AudioWorkerTimeout` retry honor `_should_retry(policy, wrapped)` per F-Plan-R7-B / `ComfyWorkerUnsupportedResponse → AudioWorkerUnsupportedResponse` immediate raise / `ComfyWorkerError → AudioWorkerError` immediate raise,均 `from exc`);`execute()` 走 `repo.put` with `ArtifactType(modality="audio", shape="waveform", display_name="audio_asset")`(F-Plan-R6-A:`shape="waveform"` 是 UE bridge `manifest_builder.py:45 _KIND_MAP[("audio","waveform")] = "sound_wave"` 唯一映射,**不**用 `cand.format`)+ `file_suffix=f".{cand.format}"` + `metadata={"format": ..., "duration_seconds": ..., "sample_rate": ..., "worker_metadata": dict(cand.metadata)}`
  - **NEW**:`config/models.yaml` + `tests/fixtures/test_models.yaml` 加 2 entry — `models.comfy_local_audio`(virtual id `comfy/local-audio` + provider=comfy_api + kind=audio + pricing=null)+ `aliases.audio_local`(preferred=[comfy_local_audio];SRS FR-MODEL-007 alias 列表第 11 项)
  - **NEW**:`runtime/failure_mode_map.py` — `FailureMode.audio_worker_timeout` + `audio_worker_unsupported`(均 `Decision.abort_or_fallback`,沿 mesh_worker_* 同模式;`AudioWorkerError` 归类 `audio_worker_unsupported`)— `classify()` 顺序:audio 子类**先于** mesh 子类**先于** generic worker_*(F-Plan-R7-B + R4-F1 priority 修订)
  - **NEW**:`runtime/dry_run_pass.py::_check_comfy_reachability` gate set 从 `{comfy/local, comfy/local-mesh}` 扩为 `{comfy/local, comfy/local-mesh, comfy/local-audio}`
  - **NEW**:`examples/comfy_local_smoke_audio.json` text-to-audio bundle(单 step `audio.t2a` + `provider_policy.models_ref="audio_local"` + `step.retry_policy={max_attempts:2, backoff:"fixed", retry_on:["timeout","provider_error"]}` 顶层 + `step.config.{worker_timeout_s, spec.{comfy_workflow:"Audio_Workflows/audio_stable_audio_example", comfy_params:{text, negative_prompt, duration_seconds:10.0, seed:42, steps:50}, comfy_lifecycle:"none"}}`)
  - **NEW**:`probes/provider/probe_comfy_audio.py` opt-in probe — `FORGEUE_PROBE_COMFY_AUDIO=1` 才跑(默认 SKIP);本地 GPU subprocess ~30-90s + 首次模型权重 HuggingFace 拉 ~2GB Stable Audio Open;模块顶层零副作用(L3 fence 守门)
  - **NEW fence 文件 + delta**:`tests/unit/test_audio_worker.py`(6 fence)+ `tests/unit/test_comfy_subprocess_audio.py`(19 fence)+ `tests/unit/test_generate_audio_comfy.py`(14 fence);`test_failure_mode_map.py` 加 6 audio fence;`test_probe_framework.py` 加 2 audio probe fence;`test_model_registry.py` 加 2 audio fence;共 +49 fence
  - **8 轮 codex review writeback**:1 design + 7 plan + S5 G6 `/codex:review --base main`(4 P2/P3 finding,全 out-of-scope pre-existing image/mesh/env/tool bugs)+ S6 G11 `/codex:adversarial-review`(3 finding:F1 high finish gate L2 hard blocker、F2 high path containment R7-C 重提、F3 medium per-candidate seed setdefault bypass);28+ findings 总计,全 writeback 或 disputed-permanent-drift with Reasoning Notes anchor;disputed_open: 0 each round。
  - **G11 finding 处理**:F1 closed by **L2 actual PASS**(用户授权改 ComfyUI workflow JSON `SaveAudioMP3 → SaveAudio` 一行 + 重跑 smoke);F2 closed by `disputed-permanent-drift`(R7-C 立场维持 + design.md `## Reasoning Notes — G11-F2 round-8` 完整记录 codex 反驳 + Claude 反驳;follow-on `comfy-agent-cli-path-containment-hardening` 三 capability 同步加);F3 closed by **fixed in this change**(`comfy_worker.py:912` `setdefault("seed", call_seed)` → `params_for_call["seed"] = call_seed` 直接覆盖;新 fence `test_generate_audio_per_candidate_seed_overrides_comfy_params_seed`;image:442 + mesh:703 同 bug 走 follow-on `comfy-worker-seed-setdefault-bug-fix`)
  - **TBD register update**:SRS §7.3 TBD-002 lift(❌→⚠️ baseline,baseline 已落地;远端 AudioCraft 待 follow-on)+ TBD-009 Phase 2 标完成(Phase 3 video 仍 follow-on)
  - **L2 evidence FULL PASS**:ComfyUI 0.9.2 user-authored workflow JSON `Audio_Workflows/audio_stable_audio_example.json` `SaveAudioMP3` 节点缺 `quality` required input(同问题影响 ACE-Step manifest);用户授权改 ComfyUI workflow JSON 一行(`SaveAudioMP3` → `SaveAudio`,匹配 manifest `outputs.primary: audio/flac` 原本声明)后重跑,run `audio_smoke_l2_pass` 真实 FLAC 1,227,925 bytes(1199.1 KB)落地 + magic bytes `b'fLaC'` + STREAMINFO 头 PASS + producer `comfy_agent_cli` / model `comfy/local-audio`(audio executor `:142` 正确 attribution,无 G6-F2/F3 image/mesh 历史 bug);evidence `archive/2026-05-03-comfy-agent-cli-audio-adoption/notes/live_smoke_audio_20260503_full.md`
  - **License 边界提示**(F6 round-2 design 写入 + CLAUDE.md):Stable Audio Open 1.0 走 Stability AI Community License(commercial use ≤ $1M annual revenue);超出阈值切 ACE-Step v1 manifest 或自审 Stability 当前 license。ForgeUE 框架不分发模型权重
  - **测试覆盖**:**pytest -q 实测 1294 passed in 62.45s**(prior 1234 baseline + 60 fence:49 audio capability + 6 G6/G11 cross-check evidence + 5 doc sync + 1 G11-F3 seed override);Level 0 forgeue_verify 全 OK;Level 2 audio live smoke FULL PASS(1.17 MB FLAC artifact)
- **ComfyUI agent CLI mesh capability adoption(Phase 1 of TBD-009)**(2026-05-03,OpenSpec change `comfy-agent-cli-mesh-audio-video-adoption`):
  - **NEW**:`ComfyAgentWorker` capability-aware dispatch — 加 `_CAPABILITY_BY_MODEL_ID` 表(`comfy/local` → image,`comfy/local-mesh` → mesh)+ 三段表 `_validate_outputs`(REQUIRED + auxiliary tolerated + REJECTED;mesh-mode `outputs.images` 是 auxiliary preview tolerated 不消费 + INFO log emit;`outputs.audio/video` rejected raise);新 public 方法 `generate_mesh(spec, source_image_filename, num_candidates, seed, timeout_s) -> list[MeshCandidate]`(round 5 D10:filename only,不是绝对路径)
  - **NEW**:`GenerateMeshExecutor` 加 `_should_use_comfy_worker_path(ctx)` + `_generate_via_comfy_worker(ctx, spec, source_image_bytes, ...)` — 沿用现有 `_resolve_source_image(ctx)` 流程(image-to-mesh DAG,B2 + D7);自带内部 retry loop 用 `policy.max_attempts`(本地非 premium,绕开 executor 主流程 `attempts=1` 强制;D9 + R2-F2);ComfyWorker 异常族(`WorkerTimeout/Error/UnsupportedResponse`)wrap 为 `MeshWorker*` 异常族 with `from exc`(让 FailureModeMap 路由正确)
  - **NEW**:`config/models.yaml` + `tests/fixtures/test_models.yaml` 加 2 entry — `models.comfy_local_mesh`(virtual id `comfy/local-mesh` + provider=comfy_api + kind=mesh + pricing=null)+ `aliases.mesh_local`(preferred=[comfy_local_mesh])
  - **NEW**:`FORGEUE_COMFY_INPUT_DIR` env var(REQUIRED for mesh path,round 5 D10)— `_generate_via_comfy_worker` 把 source bytes 写到 `Path(env)/forgeue_<sha1>.png`(NOT in-tree;`forgeue_` prefix 防与 ComfyUI 自家 input 文件冲突);ComfyUI `LoadImage` 节点接 filename only;NFR-PORT-004 适用范围调整(input 副本不算产物,GLB output 仍 in-tree)
  - **NEW**:`DryRunPass._check_comfy_reachability` gate set 从 `{comfy/local}` 扩为 `{comfy/local, comfy/local-mesh}`(P-F4 round-2 plan writeback)
  - **NEW**:`examples/comfy_local_smoke_mesh.json` image-to-mesh DAG bundle(image step `image_local` alias + mesh step `mesh_local` alias depends_on);live smoke L2 evidence GLB 真实生成 3.5 MB(`notes/live_smoke_mesh_20260503_full.md`)
  - **ADR-007 边界形式化**(round 5 D4):premium mesh 判定改用现有 `pricing.per_task_usd > 0`(NOT 新 `BudgetTracker.is_premium` API);本地 ComfyUI mesh `pricing: null` → 非 premium → 内部 retry;远端 Hunyuan3D `per_task_usd: 0.25` → premium → executor 主流程 `attempts=1` 强制 + ADR-007 strict no-silent-retry 不变。`mesh_worker_timeout/error` mode 都是 `abort_or_fallback`(R4-F1:wrapped MeshWorkerTimeout 经 FailureModeMap 仍走该 mode,本地 retry 由内部 loop 隐式实现)
  - **`MeshCandidate.metadata` provenance**(round 5 D5):新 5 keys `comfy_manifest` / `comfy_params_snapshot` / `comfy_capability` / `comfy_original_filename` / `comfy_input_filename`(WAS round 1 设计 `comfy_source_image_path`;round 5 拆为 input_filename + input_dir 反映 ComfyUI 域而非 in-tree)
  - **NEW**:`tests/unit/test_comfy_subprocess.py` 加 21 mesh fence(capability dispatch + 三段表 + repo.put 流程 + source bytes 注入 + ADR-007 边界);`tests/unit/test_generate_mesh_comfy.py` 新建 17 fence(executor dispatch + 异常 wrap + retry budget + FailureModeMap 路由 + end-to-end execute);`tests/unit/test_model_registry.py` 加 2 fence(comfy/local-mesh model + mesh_local alias);共 +40 fence
  - **ComfyUI 共享目录新增 ForgeUE 依赖**(user-authored,NOT in ForgeUE git):`D:/AI/ComfyUI/workflows/official_main_validated_api/GameAssets/03_mini_image_to_3d_hunyuan_loadimage.json`(API workflow,LoadImage 变体)+ `D:/AI/ComfyUI/scripts/comfyui_api/manifests/GameAssets/03_mini_image_to_3d_hunyuan_loadimage.json`(manifest,暴露 input_image patches);ComfyUI 重装时手工保留(否则 mesh smoke 失败)
  - **5 轮 codex review**:R1 design(4 finding,造字段)→ R2 design(4 finding,路径错 + 异常族 + spec 矛盾)→ R3 design(3 finding,FailureModeMap 优先级 + dict 访问)→ R4 design(1 finding,半回写 sweep)→ R5 plan(4 finding,commit 拆分 + fence 缺口 + ComfyUI 实机依赖 + dry_run_pass 文件归属);全 accepted-codex writeback。Round 5 D10 implementation discovery(LoadImage 节点要 ComfyUI input/ filename 不接绝对路径)+ user-authorized 方案 A 解决
  - **TBD register**:SRS §7.3 TBD-009 行更新「Phase 1 mesh 已落地」;Phase 2 audio / Phase 3 video 仍 follow-on(blocked-on TBD-002 通用 audio worker 契约 + video 输出策略决策)
  - **测试覆盖**:**pytest -q 实测 1234 通过**(commit 5 baseline,1192 prior + 38 unit fence + 3 bundle loader fence + 1 env-unset fence)。Level 0 forgeue_verify OK;Level 2 live smoke FULL PASS,GLB 真实生成 3.5 MB(`notes/live_smoke_mesh_20260503_full.md`)
- **ComfyUI worker:HTTP → agent CLI subprocess**(2026-05-02,OpenSpec change `comfy-agent-cli-adoption`):
  - **BREAKING**:`HTTPComfyWorker` 删除替换为 `ComfyAgentWorker`(`src/framework/providers/workers/comfy_worker.py`);v1 HTTP 实装(`/prompt` + `/history` + `/view`)在 commit `292420a` 之前 git history 可查
  - **BREAKING**:bundle `step.config.spec.workflow_graph` 字段废止;新协议 `step.config.spec.{comfy_workflow, comfy_params, comfy_lifecycle}`,`comfy_lifecycle` 本 change 范围内仅接受 `"none"`(D6 决议;`ensure_running` / `ensure_release` / `self_managed_session` 三种因 orchestrator `to_thread` 包装下 cancel 不可达 worker.submit 留给 SRS TBD-010 `executor-async-rewrite` 后续 change 解锁)
  - **BREAKING**:CLI flag `--comfy-url` 静默 deprecated(打印 stderr warning,fallback `FakeComfyWorker`);production `ComfyAgentWorker` 由 `GenerateImageExecutor._generate_via_worker` inline 构造从 env vars 读 `FORGEUE_COMFY_*`(round 2 OQ-6 = F-B 决议;ProviderDef schema NOT 扩展,F-A 登记 SRS TBD-011 后续 change)
  - **NEW**:`config/models.yaml` + `tests/fixtures/test_models.yaml` 加 3 entry — `providers.comfy_api`(`api_key_env: null + api_base: null` 占位)+ `models.comfy_local`(虚拟 model id `id: "comfy/local"` REQUIRED + provider=comfy_api + kind=image + pricing=null)+ `aliases.image_local`(preferred=[comfy_local], fallback=[];SRS FR-MODEL-007 alias 列表新增第 10 项)
  - **NEW**:`StepContext` 加 `run_dir: Path` 字段(round 2 OQ-7 = G-A);`Orchestrator._compute_run_dir(run)` helper 用 `getattr(self.checkpoints, "_root", None) / run.run_id`(无双重 date,round 3 H1 fix);workers 需要 in-tree copy 时(`ComfyAgentWorker` / future workers)从 `ctx.run_dir` 拿目标目录(NOT `ctx.run.artifact_dir` — 不存在)
  - **NEW**:`GenerateImageExecutor._should_use_worker_path` + sync `_generate_via_worker` worker dispatch branch — detect `prepared_routes` 含 `model == "comfy/local"` 时走 worker 路径直接调 `ComfyAgentWorker.generate(...)` sync(round 3 G4 commit 3 drift writeback:`ComfyWorker` ABC `generate` 是 sync,no asyncio.run bridge needed),bypass router 避免 LiteLLM wildcard 误路由
  - **NEW**:`DryRunPass._check_comfy_reachability` 加 conditional probe — gate by model id(`comfy/local`;round 2 G1 limitation:ResolvedRoute lacks provider field);env unset / probe failure → `report.warnings` warning(NOT hard error,G8 commit 7 drift writeback:让 `test_bundle_dry_run_passes` generic structural fence pass on CI hosts without ComfyUI;hard fail-fast invariant preserved at step time when `ComfyAgentWorker.__init__` raises `WorkerUnsupportedResponse`)
  - **NEW**:`ComfyAgentWorker.probe_sync` classmethod 给 DryRunPass 用 — sync `subprocess.run([..., "-m", "comfyui_api", "status"], cwd=scripts_dir, timeout=30)`(round 3 plan-stage codex P2 fix:NOT asyncio.run,因为 `DryRunPass.run` 是 sync 在 arun event loop 内被调用)
  - **NEW**:`tests/unit/test_comfy_subprocess.py`(26 fence)+ `tests/unit/test_step_context.py`(2 fence)+ `tests/unit/test_orchestrator.py`(2 fence)+ `tests/unit/test_fake_comfy_worker_schema.py`(5 fence)+ `tests/unit/test_model_registry.py` 加 3 fence。`tests/unit/test_comfy_http_unsupported.py` DELETED(HTTP 协议不存在);`tests/unit/test_adapter_budget_clamp.py` 删 2 个 HTTP-specific budget clamp fence
  - **NEW**:`examples/comfy_local_smoke.json` 重写(`provider_policy.{capability_required: image.generation, models_ref: image_local}` + `spec.{comfy_workflow: GameAssets/01b_singleview_sdxl, comfy_params, comfy_lifecycle: none}`);`examples/comfy/{build_bundle.py, tavern_door.api.json, image_z_image_turbo.json}` DELETED(v1 inline-workflow trio,git history 在 commit `292420a` 留作对比基线)
  - **Spec deltas**:`openspec/specs/{provider-routing, runtime-core, artifact-contract, examples-and-acceptance, probe-and-validation}/spec.md` 5 个 capability。provider-routing 加 8 ADDED Requirements + 1 MODIFIED ("Non-OpenAI protocols ship dedicated adapters" 升级到 5 协议家族 — adapter chain / injected worker / executor-side branch);runtime-core ADDED "StepContext exposes run_dir";probe-and-validation 加 ~26 fence 名单
  - **3 轮 design codex review + 3 轮 plan codex review + Lean Apply Mode**:Plan-stage codex 跑 3 轮 (round 1 → 2 → 3) collapsed plan-vs-contract drift;Apply-stage 8-commit chain 走 Lean Apply Mode (per-commit codex skip,production code review 推迟到 G11 stage `/forgeue:change-review` 一次性);所有 codex 输出 verbatim 落 `review/codex_*_review.md` + 12-key audit frontmatter
  - **TBD register**:SRS §7.3 加 TBD-009(ComfyUI mesh/audio/video workflow follow-on)+ TBD-010(executor async rewrite,let lifecycle extend to ensure_running)+ TBD-011(ModelRegistry ProviderDef.kind schema extension 后续 change)
  - **测试覆盖**:**pytest -q 实测 1184 通过**(post-G7 `test_comfy_subprocess.py` 26 fence;前基线 1144 + 36 新 fence + 5 backward-compat 调整 - 1 删除 HTTP fence 文件 = 1184)。Level 0 forgeue_verify 2/2 OK;Level 2 live smoke 待用户在装 ComfyUI 的本机跑,evidence 落 `notes/live_smoke_<date>.md`
- **Lazy artifact_store package import surface**(2026-04-27,OpenSpec change `lazy-artifact-store-package-exports`):
  - `src/framework/artifact_store/__init__.py` 由 eager re-export(23 行)改为 PEP 562 `__getattr__` + `__dir__` lazy export(~75 行),沿用同 repo `src/framework/comparison/__init__.py:50-95` 现成模板。`hashing` 子模块保持 eager(零依赖、所有 caller 都用得到);`repository` / `payload_backends` / `lineage` / `variant_tracker` 改 lazy 4 frozenset 路由 `__getattr__`;`globals()[name] = value` PEP 562 cache;`if TYPE_CHECKING:` 块辅助 mypy / pyright 静态分析;`__dir__` 函数返 `sorted(set(__all__) | set(globals()))` 保 `dir()` / `inspect.getmembers()` 公共 API 表面零变化(S2 codex F3 finding 写回)。
  - `__all__` byte-identical 保留 9 名:`ArtifactRepository` / `LineageIndex` / `PayloadBackend` / `PayloadBackendRegistry` / `PayloadTooLarge` / `VariantTracker` / `get_backend_registry` / `hash_inputs` / `hash_payload`。30+ 既有 callsite(`from framework.artifact_store import ArtifactRepository, get_backend_registry` 等)零修改,PEP 562 透明转发。
  - 新增 `tests/unit/test_artifact_store_lazy_imports.py` 4 fence:`test_import_artifact_store_does_not_pull_repository_or_payload_backends`(Scenario 1)/ `test_first_access_of_lazy_symbol_loads_submodule_and_caches`(Scenario 2,接受 cluster materialization,见 spec.md)/ `test_dir_returns_full_public_api_surface_before_any_lazy_access`(Scenario 4 `__dir__` 守门)/ `test_no_callsite_uses_submodule_path`(Scenario 3,扫 `(?:from|import) framework.artifact_store.<lazy>` 形式 + `^[ \t]*` multiline anchor 防 docstring 假阳性)。subprocess helper `_run_clean_subprocess` 注入 `PYTHONPATH=<repo>/src` + assert `__file__` 落 working-tree(S3 codex F2 finding 写回)。
  - 收紧 `tests/unit/test_run_comparison_loader.py::TestImportFence` + `test_run_comparison_cli.py::TestCliImportFence` 禁止清单加 4 prefix(`framework.artifact_store.{repository,payload_backends,lineage,variant_tracker}`);删 `comparison/cli.py` + 2 fence test 内 "transitive load is unavoidable" 段,改一句指向新 spec Requirement。
  - OpenSpec spec delta:`openspec/specs/artifact-contract/spec.md` ADDED Requirement「Package import surface is lazy-load by default」+ 4 Scenarios(包括 cluster materialization 诚实 wording,见 S3 codex F1 + S6 codex F4 writeback)。`/opsx:archive` sync-specs 时合入主 spec。
  - 4 轮 codex review 全 writeback:S2 design /codex:adversarial-review 4 findings(F1 cluster spec 诚实化 + F2 subprocess PYTHONPATH + F3 `__dir__` + F4 fence count)→ commit `5ce16c14`;S3 plan review 4 findings(F1 spec scenario 2 cluster + F2 G2 红基线 + F3 5-vs-aux file boundary 拆 + F4 3-vs-4 fence count)→ commit `5ce16c14`;S5 code review `/codex:review --base main` 5 findings 全 out-of-scope(targeting 已 archive 的 `fuse-openspec-superpowers-workflow` + `add-run-comparison-baseline-regression` 触动文件)→ 不需 writeback 仅诚实记账;S6 adversarial mixed scope 4 findings(F1 doc sync workflow ordering accepted-claude + reasoning_notes_anchor / F2 PEP 562 attribute path 诚实 / F3 fence regex `import` 形式 + multiline anchor / F4 design.md Decision 3 引文同步)→ commit `6df6c812`。所有 codex 输出 verbatim 落 `review/codex_*_review.md` 12-key audit frontmatter,Claude file:line 独立验证(沿 ForgeUE memory `feedback_verify_external_reviews`)。
  - TDD red-baseline 闭环:G2 production change 之前先 stage fence 文件,跑 against eager `__init__.py` 捕获 2/4 FAIL(`evidence/tdd_red_baseline.md`);G2 production change 后 G3 重建 fence,跑 against lazy 4/4 PASS;证明 fence 不是 green-on-arrival 假绿(S3 codex F2 writeback 合规)。
  - 测试覆盖:**pytest -q 实测 1144 通过**(post-G2/G3/G4;前基线 1140 + 4 新 fence)。Level 0 forgeue_verify 2/2 OK / 0 FAIL;Level 1/2 SKIP per opt-in env guard policy(ADR-007)。
- 关闭 `add-run-comparison-baseline-regression` 的 deferred follow-up `lazy-artifact-store-package-exports` —— 该 follow-up 在 2026-04-27 落地为独立 OpenSpec change,见上方条目;原 task 5 内 fence 与 loader fence 对齐 carve-out(只锁 9 个执行链路前缀)在本 change 收紧为 13 个前缀(原 9 + 新 4 个 artifact_store 写侧 prefix),docstring 与 fence test class docstring 内"transitive load is unavoidable"段落同步移除。

### Added
- **ForgeUE Integrated AI Change Workflow**(2026-04-27,OpenSpec change `fuse-openspec-superpowers-workflow`):
  - 中心化融合 OpenSpec(契约锚点)× Superpowers(evidence 生成器)× codex-plugin-cc(stage cross-review hook)三方工作流。OpenSpec change artifact 是唯一规范源,evidence 服务于契约,实施暴露的契约漏洞必须回写。
  - 8 个 Claude slash 命令:`/forgeue:change-{status,plan,apply,debug,verify,review,doc-sync,finish}`,每个对应 S0-S9 状态机的某个 stage,内部按需触发 Superpowers skill + codex `/codex:*` review hook + writeback 检测。
  - 5 个 stdlib-only Python 工具:`tools/forgeue_env_detect.py`(5 层 env 检测 + plugin 可用性启发式)/ `forgeue_change_state.py`(state 推断 + `--writeback-check` 4 类 named DRIFT 检测,回写检测主力)/ `forgeue_verify.py`(Level 0/1/2 编排,产 verify_report.md)/ `forgeue_doc_sync_check.py`(10 文档静态扫,标 [REQUIRED]/[OPTIONAL]/[SKIP]/[DRIFT])/ `forgeue_finish_gate.py`(中心化最后防线,evidence 完整性 + 12-key frontmatter + cross-check + writeback 真实性)。
  - 2 个 ForgeUE skills:`.claude/skills/forgeue-integrated-change-workflow/`(中心化编排器主 skill)与 `.claude/skills/forgeue-doc-sync-gate/`(Sync Gate 编排)。
  - 1 个 OpenSpec spec delta:`openspec/specs/examples-and-acceptance/spec.md` ADDED Requirement「Active change evidence is captured under OpenSpec change subdirectories with writeback protocol」(3 scenarios;P9 archive 时由 `/opsx:archive` sync-specs 合入主 spec)。
  - 1 份合并主文档:`docs/ai_workflow/forgeue_integrated_ai_workflow.md`(4 section — fusion contract / agent phase gate policy / documentation sync gate / state machine + writeback);`docs/ai_workflow/README.md` §5 / §8 同步更新表格描述。
  - 12-key audit frontmatter 协议(11 audit + 1 wrapper):每份 formal evidence 必含 `change_id` / `stage` / `evidence_type` / `contract_refs` / `aligned_with_contract` / `detected_env` / `triggered_by` / `codex_plugin_available` 8 个 always-required key,`drift_decision` / `writeback_commit` / `drift_reason` / `reasoning_notes_anchor` 4 个 conditional key 在 `aligned_with_contract: false` 时必填。
  - 4 类 DRIFT taxonomy:`evidence_introduces_decision_not_in_contract` / `evidence_references_missing_anchor` / `evidence_contradicts_contract` / `evidence_exposes_contract_gap`;由 `forgeue_change_state.py --writeback-check` exit 5 触发。
  - 工作流内禁令:不调 `/codex:rescue`(单点修复 helper,与 stage gate 协议正交);不启 codex review-gate hook(stage gate 与 review-gate 重复且常冲突,由 `forgeue_finish_gate` WARN 提示);不让 evidence 取代 contract 作规范源。
  - 测试覆盖:262 P4 fence(`tests/unit/test_forgeue_*.py`)+ 13 P4 codex-review fixup fence(diff_engine stable-key + finish_gate 8 always-required key + env_detect SSE_PORT + doc_sync_check non-core detection)+ 3 P5 verify subprocess env fence(PYTHONPATH=src 注入)= pytest baseline 1126(848 P3 + 278)。
- **Run Comparison / 基线回归**(2026-04-25,OpenSpec change `add-run-comparison-baseline-regression`):
  - 新增模块 `src/framework/comparison/`,含 `models.py`(`RunComparisonInput` / `ArtifactDiff` / `VerdictDiff` / `MetricDiff` / `StepDiff` / `RunComparisonReport`,`schema_version="1"`)/ `loader.py`(只读消费,异常族 `RunDirNotFound` / `RunDirAmbiguous` / `RunSnapshotCorrupt` / `PayloadMissingOnDisk`)/ `diff_engine.py`(纯函数 `compare()`,sparse `summary_counts`)/ `reporter.py`(`render_json` + `render_markdown` + `write_reports`,固定文件名 `comparison_report.json` + `comparison_summary.md`,ASCII-only)/ `cli.py` + `__main__.py`(CLI 入口)。
  - CLI 入口:`python -m framework.comparison --artifact-root <root> --baseline-run <id_a> --candidate-run <id_b> [--output-dir <out>]`。Exit codes 0 / 2(RunDir 定位失败 / schema 损坏)/ 3(strict 模式 payload 缺失)/ 1(其他未识别异常)。`--no-hash-check` / `--non-strict` / `--json-only` / `--markdown-only` / `--quiet` flags 均见 `--help`。
  - 离线 fixture:`tests/fixtures/comparison/builders.py::build_fixture_pair(root)` deterministic 构造 baseline / candidate run 目录(覆盖 unchanged / content_changed / metadata_only ArtifactDiff + run-level cost_usd MetricDiff + lineage_delta `transformation_kind`)。
  - 测试覆盖:`tests/unit/test_run_comparison_{models,loader,diff_engine,reporter,cli}.py`(共 ~295 unit 用例)+ `tests/integration/test_run_comparison_cli.py`(4 用例,含 happy path、不污染 `<repo>/demo_artifacts/` 递归快照、lineage diff 端到端、`examples/mock_linear.json` + FakeAdapter 双跑离线集成)。pytest -q 实测 848 通过(基线 549 + Run Comparison ~299)。
  - Codex Review Gate 双轮 PASS(Task 4 / 5 / 6 各两轮);Task 5 第一轮捕获 stdout/stderr ASCII-safe Blocker(由 `_console_safe` + 13 个新测试解决);Task 6 第一轮捕获 spec validation gate 缺失 Blocker(由 FakeAdapter 双跑 + `_snapshot_tree` 递归解决)。
  - **Deferred follow-up**:`lazy-artifact-store-package-exports`(尚未创建 OpenSpec change)。Task 5 实装时发现 `framework.comparison.loader` 顶层 `from framework.artifact_store.hashing import ...` 必然触发 `framework/artifact_store/__init__.py` 执行,而该 `__init__` 当前 eager-import `repository` / `payload_backends`,导致这两个模块作为 transitive 出现在 `sys.modules` 里。当前 fence 与 Task 2 loader fence 对齐(只锁 9 个执行链路前缀);**未**修改 `artifact_store/__init__.py`,跨子系统改动留后续独立 change 评估 PEP 562 lazy export 方案。详见 `openspec/changes/add-run-comparison-baseline-regression/tasks.md` §"Deferred Follow-ups"。
- **TBD-008**(Codex B+C 分层)`tests/fixtures/review_images/` 目录 + `tavern_door_v{1,2,3}.png` 真 Qwen PNG 加上 `tests/fixtures/__init__.py.load_review_image()` helper,供视觉 review 测试复用;repo 增 ~4.4MB(一次性)
- **TBD-008** `probes/provider/probe_visual_review.py` — opt-in `FORGEUE_PROBE_VISUAL_REVIEW=1` probe,对比 `review_judge`(Anthropic Opus 4.6)vs `review_judge_visual`(GLM-4.6V)对同 3 张真图的打分分布,落 `comparison_table.md`;首跑确认 Anthropic 判别度更高(0.62-0.88 跨度),GLM 打分更压缩(0.80-0.95)
- **TBD-008** `tests/integration/test_l4_image_to_3d.py` 新增 2 条 fence(Codex Phase G 两轮 review 后):
  - `test_l4_mesh_reads_selected_candidate_from_review_verdict` — **真实生产路径**,验证 mesh 从 `report.verdict.selected_candidate_ids[0]` 读图
  - `test_l4_mesh_resolves_selected_image_from_selected_set_bundle` — forward-compat,守 SelectExecutor 流程的 `bundle.selected_set` 路径
- **TBD-007** `probes/provider/probe_hunyuan_3d_query.py` — read-only /query probe for historical Hunyuan 3D job_ids(opt-in `FORGEUE_PROBE_HUNYUAN_3D=1`,接受 `--job-id` repeated flag);用于失败后查 server 端 job 真实状态,避免 blind retry 双扣
- **TBD-007** `tests/unit/test_mesh_no_silent_retry.py` (4 fences) + `tests/integration/test_mesh_failure_visibility.py` (1 fence)
- Modern Python project layout: `src/framework/` (PEP 621 src layout)
- `src/framework/py.typed` marker — declares package ships type information (PEP 561)
- `tests/conftest.py` centralized: pinned test ModelRegistry + repo-root sys.path + `stub_hydrate_env` fixture
- `[tool.ruff.lint]` balanced rule set (E/F/W/I/B/UP/SIM/RUF) + per-file ignores for tests/probes
- `[tool.ruff.format]` configuration replacing black
- `[tool.mypy]` baseline configuration (non-strict, third-party `ignore_missing_imports`)
- `[tool.coverage]` source/branch/omit/exclude_lines configuration
- `LICENSE` (MIT)
- `.editorconfig` for cross-IDE indent / line-ending / encoding consistency
- `.pre-commit-config.yaml` (ruff check + ruff format + standard hygiene hooks)
- `probes/` package: handler scripts moved out of repo root into `probes/{smoke,provider}/`
- `probes/_output.py` `probe_output_dir(tier, name)` helper for consistent output paths
- `probes/README.md` — probe authoring conventions
- Documentation five-piece set: `docs/{requirements/SRS.md,design/HLD.md,design/LLD.md,testing/test_spec.md,acceptance/acceptance_report.md}`
- `docs/INDEX.md` — documentation entry navigation
- `docs/archive/README.md` — historical document index
- `AGENTS.md` — AI agent collaboration context (mirror of `CLAUDE.md`)

### Changed
- **TBD-008 — visual review 契约 / 质量分层**(Codex 独立 review 指出盲区 + 采纳 B+C 分层):
  - `tests/integration/test_p2_standalone_review.py::test_p2_visual_mode_attaches_image_bytes_to_judge_prompt` 升级:`VISUAL_A/B/C` 伪字节 → fixture 真 PNG;FakeAdapter 打分从 "按位置" 改为 "按 candidate_id" 映射;新增 winner/confidence 断言 + JPEG 压缩路径断言
  - `tests/integration/test_p3_production_pipeline.py` 升级:`ORIGINAL_` / `REVISED_` / `API_` / `OK` prefix 伪字节 → fixture 真 PNG;revise + api_path + worker_timeout 三条路径全走真 PNG;原有断言保留
  - `tests/integration/test_l4_image_to_3d.py::_seed_image_artifact` helper 升级:`fake-source-image-bytes` → fixture 真 PNG;所有 L4 mesh 测试自动受益
  - `a2_image` / `a2_review` bundle 的"视觉 review 证据力"在 `docs/acceptance/acceptance_report.md` §6.2 修订:明确标为 "text-only / schema smoke",真视觉证据在 `test_p2/p3/l4` integration + `probe_visual_review.py`
  - `src/framework/runtime/executors/generate_mesh.py::_resolve_source_image` 优先级重写(Codex Phase G R1+R2):**verdict > selected_set > 直接 image > candidate_set**。之前扁平 image 优先导致真实 workflow 静默取 cand_0 无视 review verdict;本次修复让 mesh 读 verdict 选中的候选
  - 测试基数 541 → 543(2 新 fence + 3 翻转不变数)
- **TBD-007 — mesh 重试塌缩**(Codex 独立 review 协助找出第 4 层): 用户实测 1 个 mesh job 在腾讯云控制台被扣 16 调用 × 20 积分 = 320 积分,根因是 4 层叠加重试(L1 `_apost` transient × L2 `GenerateMeshExecutor` 内部循环 × L3 orchestrator `worker_*` retry × L4 download Range resume)。修法:
  - `mesh_worker._apost` 移除 `with_transient_retry_async` wrapper(L1 拆掉)
  - `GenerateMeshExecutor` 对 `capability_ref="mesh.generation"` 强制 attempts=1(L2 短路)
  - `failure_mode_map` 新增 `mesh_worker_timeout` / `mesh_worker_error` mode → `Decision.abort_or_fallback`,classify 优先匹配 mesh 子类(L3 改路由)
  - `MeshWorkerError` / `MeshWorkerTimeout` 加 `(*, job_id, worker, model)` kwargs;`_atokenhub_*` 失败处填字段
  - `orchestrator` failure_event 写入 `context.{job_id, worker, model}`
  - `framework/run.py` mesh 失败时 stderr 提示用户先跑 `probe_hunyuan_3d_query --job-id <...>` 查 server 端 job 状态(避免双扣已完成 job),再决定 `--resume`
  - HYPOTHESIS 验证(probe_hunyuan_3d_query):abandoned mesh job 后台仍生成完成,blind retry 真双扣
  - 5 条新 fence + 3 条翻转(原"重试 2 次成功" → 现"单次 raise");测试基数 536 → 541
- Bumped `requires-python` from `>=3.11` to `>=3.12` (project actually uses 3.12+ features: `match`/`asyncio.TaskGroup`)
- `--artifact-root` CLI default now auto-buckets by date: `artifacts/<YYYY-MM-DD>`
- All probe scripts now write to `./demo_artifacts/<YYYY-MM-DD>/probes/<tier>/<name>/<HHMMSS>/`
- `[tool.ruff] target-version` bumped to `py312`
- Documentation references updated from `framework/` to `src/framework/` paths
- `claude_unified_architecture_plan_v1.md` moved from `docs/` to `docs/archive/`; demoted from authoritative to historical reference (ADR-005)

### Removed
- `framework/providers/_download.py` (sync download path; only async survives)
- Empty placeholder dirs: `framework/ue_bridge/{execute,plan}/`, `framework/workflows/templates/`
- 9 `probe_*.py` scripts removed from repo root (relocated to `probes/`)

### Fixed
- 21 Codex audit fixes covered by `tests/unit/test_codex_audit_fixes.py` (29 new fence tests)
  - FR-LC-006/007/008: cross-process Artifact metadata persistence + length-mismatch cache miss
  - FR-REVIEW-009: SelectExecutor bare-approve / explicit-reject semantics
  - FR-WORKER-009/010: tokenhub poll timeout clamp + 200/non-JSON wrap as `unsupported_response`
- TBD-006 visual review image compression (acceptance_report §6.5; Codex independent review co-authored).
  Two bugs co-fixed:
  - **Bug A**: `_build_candidates` placed raw image bytes into `CandidateInput.payload`,
    rendered through `json.dumps(default=str)` as `b'\x89PNG\\xNN...'` repr (~4x inflation).
    Now image candidates carry a metadata summary; raw bytes flow only via `image_bytes`.
  - **Bug B**: visual_mode base64-inlined unbounded image_url blocks. New
    `framework.review_engine.image_prep.compress_for_vision` (Pillow + EXIF transpose +
    768px thumbnail + alpha flatten + JPEG q=80) wired into `_attach_image_bytes`,
    raw < 256KB short-circuits to preserve Anthropic small-image path.
  - Pillow added to `[project.optional-dependencies].llm` extras (lazy import).
  - 10 new fences: `tests/unit/test_visual_review_image_compress.py` (8) +
    `tests/unit/test_review_payload_summarization.py` (2)

## [0.1.0] - 2026-04-22

### Added
- Initial baseline: P0–P4 main pipeline, L1–L4 capabilities, F1–F5 runtime features, Plan C async
- 491 passing tests (unit + integration)
- Provider integrations: LiteLLM (OpenAI-compat / Anthropic), DashScope (Qwen), Hunyuan tokenhub (Image + 3D), MiniMax, ComfyUI HTTP, Tripo3D scaffold
- UE Bridge `manifest_only` mode: `UEAssetManifest` + `UEImportPlan` + `Evidence` file contract
- WebSocket progress server (`framework.server.ws_server`)
- `framework.pricing_probe` CLI for provider pricing automation (httpx + playwright backends)

[Unreleased]: https://example.com/releases/unreleased
[0.1.0]: https://example.com/releases/0.1.0
