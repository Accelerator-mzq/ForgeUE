# Target

OpenSpec change comfy-agent-cli-adoption S3 plan-stage review.

# Context

This is the plan-stage review (S3 to S4-S5 transition for /forgeue:change-apply).
- Design-stage went through 3 codex review rounds (S2 to S3): commit a45d30b round 1 + 53397b2 round 2 + 85a0f5e round 3 (total 15 findings; 9 fixed-correctly + 6 fixed-with-caveat absorbed by drift protocol).
- This pass validates execution_plan.md + micro_tasks.md before Superpowers executing-plans + TDD writes real code.

Goal: surface any plan-vs-implementation gap that would cause apply-stage cascade failure (test red after commit X but before commit Y, or grep miss leaving callsites unpatched, or async/sync interaction unsafe in actual execution).

# Files to read first

Plan artifacts:
- execution/execution_plan.md (File Structure + Task Group Map G1-G11; Boundary Check rules; TDD discipline; Risk register linking design.md Risks)
- execution/micro_tasks.md (G1-G11 step-by-step; sketch code blocks for Steps 3.2 / 4.3 / 5.2; anchor headers reference tasks.md sections post-round-3)
- review/plan_cross_check.md (Claude self-assessment of plan, frozen ## A)

Contract reference (already-validated):
- design.md (D6/D7/D8/D9 decisions; D-FutureScope TBD-009/010/011; Risks adjusted post-round-3)
- tasks.md (11 task groups, 8-commit chain, post-round-3 anchor numbering)
- specs/provider-routing/spec.md (7 ADDED + 1 MODIFIED Requirements; round-3 H1/H2/H4/H5 fixes incorporated)
- specs/runtime-core/spec.md (NEW delta: StepContext.run_dir injection)
- specs/probe-and-validation/spec.md (~22 fence list)
- specs/artifact-contract/spec.md + specs/examples-and-acceptance/spec.md (unchanged from round 1)

Round 1+2+3 evidence chain:
- review/codex_design_review.md + codex_design_review_round_2.md + codex_design_review_round_3.md
- review/design_cross_check.md + design_cross_check_round_2.md + design_cross_check_round_3.md

# Plan-stage adversarial focus

QUESTION 1 — Are the implementation steps in micro_tasks.md actually runnable?

Specifically interrogate Claude's self-identified weaknesses in plan_cross_check.md ## A:

- W-PlanWorkloadOverflow: 11 task groups in single apply session may exceed token budget. Plan does not specify resume points. Should commits be independently verifiable so the apply work can split across sessions?
- W-ExistingTestRegression: G5 Step 5.3 says "grep all StepContext( callsites and add run_dir=tmp_path"; but the actual count is unknown and could be 30+. If patches are incomplete, ~30 tests fail in a non-obvious way.
- W-DryRunIntegration: G4 Step 4.4 says DryRunPass detects prepared_routes containing comfy/local. Verify that DryRunPass actually exposes prepared_routes at the right hook point (read src/framework/runtime/dry_run_pass.py).
- W-WorkerSignaturePythonValidate: Step 3.2 keyword-only signature is plan text only — has anyone actually parsed it through Python? Read the proposed signature literally and verify it is valid Python 3.12 syntax.
- W-AsyncRunNestedLoop: asyncio.run inside a thread (which is what asyncio.to_thread creates) — verify by reading generate_image.py:295 that the existing _generate_via_router pattern actually works, then determine if the same pattern is safe for _generate_via_worker.
- W-CommitOrderAtomicity: 8-commit chain order has G4 (executor uses ctx.run_dir) before G5 (StepContext adds run_dir). Will pytest be red at commit-3 head (G4 done, G5 not yet)? Should G5 be before G4?

QUESTION 2 — Does the Boundary Check rule actually catch real out-of-scope diffs?

The Boundary Check (execution_plan.md "Boundary Check Rules") says any git diff outside the implementation files table triggers STOP. But the implementation files table lists 8 specific paths; if implementer touches a file path not in the table (e.g. needs to modify src/framework/runtime/run.py to inject artifact_root through a different method), what happens? Is the Boundary Check truly enforceable, or is it documentation-only?

QUESTION 3 — Is there any plan-implementation gap that cannot be fixed by drift_decision protocol?

Decision Y (round 3) accepted that residual implementation-layer gaps surfaced during apply will be handled by drift_decision: written-back-to-design protocol. But are there any plan-vs-implementation gaps that this protocol cannot handle? For instance: a fundamental interface mismatch where the plan assumes ComfyAgentWorker.submit returns list[ImageCandidate] but actual ImageCandidate dataclass has changed fields, breaking dozens of downstream callsites.

# What to return

For QUESTION 1, return per-W verdict:
- W-PlanWorkloadOverflow: actionable / acknowledged-risk / blocker
- ... same for W-ExistingTestRegression / W-DryRunIntegration / W-WorkerSignaturePythonValidate / W-AsyncRunNestedLoop / W-CommitOrderAtomicity

For QUESTION 2, single verdict (boundary check enforceable / advisory-only / vague-needs-rework).

For QUESTION 3, summary of any plan-implementation gap NOT covered by drift_decision protocol (with severity).

If new plan-stage findings (call them P1, P2, ... using P prefix to distinguish from F/G/H rounds), report them in standard adversarial-review style.

End with a counter and a recommendation (proceed-to-apply / rework-plan / abort-and-restructure).

Do NOT re-raise round 1/2/3 design-stage findings — they have been writeback-closed via Decision Y.
