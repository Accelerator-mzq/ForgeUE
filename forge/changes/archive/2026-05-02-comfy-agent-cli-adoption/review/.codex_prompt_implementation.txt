# Target

OpenSpec change comfy-agent-cli-adoption — final implementation review (S5 → S6 codex review on production code, full G2-G10 commit chain).

# Context

This is the final code review pass before archive. design / plan stages already passed 6 codex review rounds (3 design + 3 plan); apply stage ran in Lean Apply Mode (per-commit codex skipped, consolidated to this stage).

# Commit chain to review (G2-G10 production code)

Base: 25b0c5c (plan rework round 3 Q3 residue fix, plan-stage end)
HEAD: 6ad798c (G10 doc sync)

Apply commits:
- 9d654d6 G2 commit 1: Registry config + 3 fence (config/models.yaml + tests/unit/test_model_registry.py)
- 592eb43 G3 commit 2: StepContext.run_dir + Orchestrator._compute_run_dir (base.py + orchestrator.py + 4 fence)
- f1e790c G4 commit 3: ComfyAgentWorker rewrite (comfy_worker.py rewrite 336->470 lines + 4 cascade cleanup files + delete test_comfy_http_unsupported)
- 07c0faa G5 commit 4: Executor _generate_via_worker + DryRunPass _check_comfy_reachability (generate_image.py + dry_run_pass.py)
- 89243e4 G6 commit 5: FakeComfyWorker conditional v2 schema gate fence (5 fence + Lean Apply Mode doc)
- 9a1e6dc G7 commit 6: test_comfy_subprocess.py 26 fence
- 29ad60d G8 commit 7: examples rewrite + delete v1 trio + dry-run drift writeback (warning not error)
- 6ad798c G10 commit 8: doc sync (SRS + CHANGELOG + CLAUDE + acceptance + TBD-009/010/011)

# Adversarial focus

Final implementation review focusing on production code quality + hidden bugs that previous design/plan stages could not catch.

QUESTION 1 — Are there hidden runtime bugs in the production code?

Examine particularly:

- ComfyAgentWorker subprocess invocation (comfy_worker.py): subprocess.run with timeout edge cases, JSON parsing error paths, file copy failure scenarios (disk full / permission denied / source file missing), Windows path handling (UNC, 260-char limit, non-ASCII filename), environment variable race conditions
- GenerateImageExecutor._generate_via_worker (generate_image.py): retry loop interaction with worker dispatch (does retry policy apply correctly to ComfyAgentWorker errors?), exception class hierarchy (does WorkerUnsupportedResponse propagate correctly through except WorkerError catch?), env var read timing (read once or per retry?)
- DryRunPass._check_comfy_reachability (dry_run_pass.py): warning-not-error decision (G8 commit 7 drift writeback) is correct for generic test_bundle_dry_run_passes, but does it properly fail Run when env present + probe genuinely fails (not just unset)?
- StepContext.run_dir + Orchestrator._compute_run_dir: integration with checkpoint store, cross-process resume scenarios (does resume reuse correct run_dir?), test-mock convenience default Path('.') leakage to production
- Test fixture parallel update: tests/fixtures/test_models.yaml mirrors config/models.yaml but timestamps may diverge — is this OK, or does it create maintenance burden?

QUESTION 2 — Are there contract / spec drifts not yet written back?

Check:

- Apply stage drift writebacks: G3 commit 2 (StepContext default factory) + G4 commit 3 (sync ABC ComfyWorker.generate, no asyncio.run bridge needed) + G8 commit 7 (dry-run warning not error). Are these drifts fully documented in design.md / spec / tasks?
- Test fixture comfy_api / comfy_local / image_local entries: was test_models.yaml update mentioned in tasks.md? If not, register in design.md as "test fixture parallels config/models.yaml registry update" pattern
- examples/comfy_local_smoke.json: capability_required: image.generation field required — was this called out in tasks.md / micro_tasks?

QUESTION 3 — Are there security / robustness issues?

- comfy_params is dict from user-supplied bundle JSON, then json.dumps'd into CLI argv string. Any injection risk via specially-crafted keys (e.g. "key with space" or unicode ASCII)?
- subprocess.run with cwd=scripts_dir: if user's FORGEUE_COMFY_SCRIPTS_DIR is path with shell metacharacters, what happens?
- shutil.copy2 from D:/AI/ComfyUI/outputs/... to artifacts_dir/comfy/: is there any way for ComfyUI subprocess to overwrite an unrelated artifacts file (e.g. via symlink in outputs.images)?

QUESTION 4 — Are there missing fences for known-fragile contract points?

Compare specs/probe-and-validation/spec.md fence list vs tests/unit/test_comfy_subprocess.py + test_step_context.py + test_orchestrator.py + test_fake_comfy_worker_schema.py + test_model_registry.py (delta). Any spec-required fence missing from implementation?

# What to return

Findings in standard adversarial-review style:
- finding id (R1, R2, ... — use R prefix to distinguish from F/G/H/P/Q rounds)
- severity (blocker / high / medium / low)
- claim (file:line)
- evidence
- suggested fix

End with finding count + severity distribution + recommendation (proceed-to-archive / writeback-then-archive / blocker-stop).

Prior cross-checks (do NOT re-raise):
- F1-F6 (round 1 design, all closed)
- G1-G4 (round 2 design, all closed)
- H1-H5 (round 3 design, all closed)
- P1-P4 (round 1 plan, all closed via Q1-Q3 sweep + Q3 follow-up)
- Q1-Q3 (round 2/3 plan, all closed)

User has accepted "Lean Apply Mode" — per-commit codex review skipped during apply stage; this is the consolidated production code review.
