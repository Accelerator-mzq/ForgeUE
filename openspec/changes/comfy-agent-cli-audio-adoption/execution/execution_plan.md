---
change_id: comfy-agent-cli-audio-adoption
stage: S2
evidence_type: execution_plan
contract_refs:
  - proposal.md
  - design.md
  - tasks.md
  - specs/provider-routing/spec.md
  - specs/runtime-core/spec.md
  - specs/artifact-contract/spec.md
  - specs/examples-and-acceptance/spec.md
  - specs/probe-and-validation/spec.md
  - review/codex_design_review.md
  - review/design_cross_check.md
  - notes/audio_subprocess_probe_20260503.md
detected_env: claude-code
triggered_by: /forgeue:change-plan (Superpowers writing-plans skill methodology, generated by Claude after 1 round of codex adversarial review on contract — all 6 findings accepted-codex + writeback complete)
codex_plugin_available: true
created_at: 2026-05-03T19:30:00+08:00
aligned_with_contract: true
drift_decision: null
writeback_commit: null
drift_reason: null
reasoning_notes_anchor: null
note: |
  本 execution_plan 是 /forgeue:change-plan S2→S3 阶段产出,基于 1-round codex review
  收敛后的 contract(proposal/design/specs/tasks 已 round-2 全部 writeback)。引用的所有
  tasks.md#X.Y 锚点来自 post-round-2 contract(13 task group + ~75 sub-tasks)。任何
  implementation 越界或暴露契约缺口必须回写到 design.md / tasks.md / spec(4 类 DRIFT
  taxonomy in CLAUDE.md),不得在本计划中生成新规范源。同伴文件 micro_tasks.md 提供
  TDD 步骤级展开。

  Round 1 codex review 收敛轨迹:
  - R1: 6 finding(2 high + 4 medium)→ 全部 accepted-codex + writeback
    - F1 high: step.kind → capability_ref(runtime-core spec / examples bundle / tasks / proposal)
    - F2 high: retry/wrap 三 except 块拆分(design D9 / tasks §5.2 伪代码)
    - F3 medium: AudioCandidate 顶层字段统一(design D5 反转 + spec/artifact-contract / tasks §2.2)
    - F4 medium: outputs.metadata.audio 路径修正 + S3 阻塞 probe(notes/audio_subprocess_probe_20260503.md probe 完成)
    - F5 medium: magic bytes 强制(design D10 反转 + spec/provider-routing 加 Step 4 + tasks §4.4 加 7 fence)
    - F6 medium: Stable Audio license note(design Risks + tasks §10.4)

  最关键 invariant(违反必须 STOP + 回写 design):
  - **D3 audio-only**(远端 AudioCraft / ComfyUI video 各自 follow-on change,本 change 不接)
  - **D5 顶层字段**(`AudioCandidate.duration_seconds: float | None`,executor 持久化只从顶层读;**不**双源)
  - **D7 text-to-audio**(`GenerateAudioExecutor` SHALL NOT call `_resolve_source_image`;无 `FORGEUE_COMFY_INPUT_DIR`)
  - **D9 三 except 块拆分**(timeout 才 retry;unsupported / generic 立即 raise from exc;**不**裸 raise — 违反 R4-F1 失败模式映射)
  - **D10 magic bytes 强制**(扩展名 + magic 二次校验,不一致 raise;沿 FR-WORKER-006 mesh GLB magic)
  - **F1 真实 Step 模型**(`Step.type=StepType.generate` + `capability_ref="audio.t2a"`;**不**新增 StepType,**不**改 loader)
---

# ComfyUI Agent CLI Audio Capability Adoption — Implementation Plan

> **★ CONTRACT IS THE SOURCE OF TRUTH ★** — 本 `execution_plan.md` 与同伴 `micro_tasks.md` 是 OpenSpec contract(`proposal.md` + `design.md` + `tasks.md` + `specs/*/spec.md`)的**衍生视图**。当本 plan 与 contract 冲突时,**始终优先 contract**。Plan 已经过 1 轮 codex design review(R1 6 finding 全部 writeback 完成);若 implementer 在实施中发现新 contract gap,**STOP 并回写**到 design.md / tasks.md / spec(4 类 DRIFT taxonomy)。
>
> **For agentic workers:** REQUIRED SUB-SKILL — Use `superpowers:subagent-driven-development`(推荐)或 `superpowers:executing-plans` 实施本计划。Steps 在 `micro_tasks.md` 用 checkbox(`- [ ]`)语法。每个 task header 引用 `tasks.md#X.Y` 锚点 — 不要添加未锚定的 task;若实施需要超出锚点,**STOP 并回写**到 `tasks.md`。

**Goal:** 在已归档 `2026-05-03-comfy-agent-cli-mesh-audio-video-adoption`(image+mesh)基础上,扩展 `ComfyAgentWorker` 接 ComfyUI 本地 audio capability(text-to-audio 路径)+ **同步建立 audio worker baseline**(ABC + Candidate + 异常树 + GenerateAudioExecutor + audio.t2a capability_ref),sync lift SRS TBD-002。复用 image+mesh change 已建立的 CLI subprocess + bundle alias + env vars 基础设施;新增 audio-mode capability dispatch + audio executor worker path + 强制 magic bytes 二次校验 + 异常族 wrap。远端 AudioCraft / ComfyUI video 各自拆 follow-on change(D3)。

**Architecture:** 双子系统改造(`provider-routing` 主,`runtime-core` / `artifact-contract` / `examples-and-acceptance` / `probe-and-validation` 配套)+ **新建 audio worker baseline 子系统**(`framework.providers.workers.audio_worker` + `framework.runtime.executors.generate_audio`)。改 [`src/framework/providers/workers/comfy_worker.py`](src/framework/providers/workers/comfy_worker.py)(`_CAPABILITY_BY_MODEL_ID` / `_REQUIRED_OUTPUT_KEY` / `_AUXILIARY_OUTPUT_KEYS_BY_CAP` / `_REJECTED_OUTPUT_KEYS_BY_CAP` 4-dict 扩 audio + 新方法 `generate_audio` 含强制 magic bytes 二次校验)+ 新 [`src/framework/providers/workers/audio_worker.py`](src/framework/providers/workers/audio_worker.py)(AudioCandidate dataclass + AudioWorker ABC + 3 异常类 + FakeAudioWorker)+ 新 [`src/framework/runtime/executors/generate_audio.py`](src/framework/runtime/executors/generate_audio.py)(GenerateAudioExecutor with `step_type=StepType.generate, capability_ref="audio.t2a"` + `_should_use_comfy_worker_path` + `_generate_via_comfy_worker` 含 F2 三 except 块 retry/wrap)+ [`src/framework/runtime/failure_mode_map.py`](src/framework/runtime/failure_mode_map.py)(加 `audio_worker_timeout` / `audio_worker_unsupported` mode + `from_exception` priority 优先于 generic)+ [`config/models.yaml`](config/models.yaml) 加 2 entry(`models.comfy_local_audio` + `aliases.audio_local`)+ [`src/framework/run.py`](src/framework/run.py) 注册 `GenerateAudioExecutor` 到 ExecutorRegistry + [`src/framework/runtime/dry_run_pass.py`](src/framework/runtime/dry_run_pass.py) `_check_comfy_reachability` gate 扩 `comfy/local-audio` + 新 bundle [`examples/comfy_local_smoke_audio.json`](examples/comfy_local_smoke_audio.json)(单 audio.t2a step,no DAG)。**NO** `Step` model 改动 / `ComfyWorker` ABC 改动 / `StepType` 枚举扩 / `loader.py` step-kind 表(此表本不存在)/ `BudgetTracker.is_premium` 新 API。框架侧契约(`MeshWorker` / `ImageCandidate` / `repo.put` / FR-COST 接口)零破坏。

**Tech Stack:** Python 3.12+,标准库 `subprocess` + `shutil` + `pathlib` + `hashlib` + `logging`;无新 Python 依赖(magic bytes 校验是字节比较,不需 mutagen)。Test runner:`pytest -q`(post-change baseline = 1234 实测 + 实测增量 ~35 fence,**不**硬编码)。运行时假设 `D:/AI/ComfyUI/scripts/` + `python -m comfyui_api list / params / run` 可用(由 dry-run probe 强制校验);本机至少 1 个 audio manifest(产 `outputs.audio`,无 auxiliary set)。L2 evidence 需要 ComfyUI server running + Stable Audio Open 1.0 模型权重缓存(首次 ~2GB HuggingFace 拉)。

---

## Scope Check

Single change scope(`framework.providers.workers.audio_worker` 新建 + `comfy_worker.py` 4-dict 扩 audio + `generate_audio.py` 新建 + `failure_mode_map.py` 加 mode + `config/models.yaml` 2 entry + bundle JSON + ExecutorRegistry 注册)。**Out-of-scope**(`proposal.md` Non-Goals + `design.md` §Goals/Non-Goals):

- **远端 AudioCraft worker 协议**(D3 split,follow-on change `audio-worker-audiocraft-adoption`;本 change 仅 ABC baseline + ComfyUI 第一客户)
- **ComfyUI video capability**(Phase 3 follow-on `comfy-agent-cli-video-adoption`)
- **ComfyUI lifecycle `ensure_running` / `ensure_release` / `self_managed_session`**(SRS TBD-010)
- **audio-to-audio source bytes 路径**(类比 mesh 的 image-to-mesh;本 change 仅 text-to-audio)
- **audio metadata parser**(`duration_seconds` / `sample_rate` 解析 mutagen / wave,F4 round-2:本 change scope=None always;follow-on `audio-metadata-parser` 加)
- **audio review rubric**(类比 `ue_asset_quality.yaml` 但针对 audio;follow-on `audio-review-rubric-adoption`)
- **`Step` model 字段扩**(D5/F1 锁定:沿用 `Step.type=StepType.generate` + `capability_ref`,**不**新增 StepType 枚举值)
- **`ComfyWorker` ABC 改动**(audio 走 `ComfyAgentWorker.generate_audio` 是 ad-hoc method,**不**改 ABC `generate` 签名)

---

## File Structure

### Implementation files(production code + new fence test + bundle JSON,ride together as the writeback PR diff)

| File | Operation | Responsibility | tasks.md 锚点 |
|---|---|---|---|
| [`src/framework/providers/workers/audio_worker.py`](src/framework/providers/workers/audio_worker.py) | Create | AudioCandidate dataclass(`data: bytes` / `format: Literal["flac","mp3","wav"]` / `metadata: dict` / `duration_seconds: float \| None = None` / `sample_rate: int \| None = None`)+ AudioWorker ABC + 异常树(`AudioWorkerError` / `Timeout` / `UnsupportedResponse`)+ FakeAudioWorker fixture(minimal valid FLAC bytes,~50 bytes 不依赖第三方 codec)| §2.1 / §2.2 / §2.3 / §2.4 / §2.5 |
| [`src/framework/providers/workers/comfy_worker.py`](src/framework/providers/workers/comfy_worker.py) | Modify | `_CAPABILITY_BY_MODEL_ID` / `_REQUIRED_OUTPUT_KEY` / `_AUXILIARY_OUTPUT_KEYS_BY_CAP` / `_REJECTED_OUTPUT_KEYS_BY_CAP` 4-dict 扩 audio entry;新 method `generate_audio(spec, num_candidates, seed, timeout_s) -> list[AudioCandidate]`(NOT part of `ComfyWorker` ABC;含 F5 强制 magic bytes 二次校验);`__init__` 守门错误消息列表自动包含 `comfy/local-audio` | §4.1 / §4.2 / §4.3 |
| [`src/framework/runtime/executors/generate_audio.py`](src/framework/runtime/executors/generate_audio.py) | Create | `GenerateAudioExecutor` with `step_type = StepType.generate, capability_ref = "audio.t2a"`(F1 round-2);`_should_use_comfy_worker_path(ctx)` + `_generate_via_comfy_worker` 含 F2 三 except 块拆分 retry/wrap(timeout 才 retry,unsupported/generic 立即 raise from exc);`execute` 通过 `ctx.repository.put(file_suffix=f".{cand.format}", metadata={"format": ..., "duration_seconds": None, "sample_rate": None, "worker_metadata": ...})` 持久化 | §5.1 / §5.2 |
| [`src/framework/runtime/executors/__init__.py`](src/framework/runtime/executors/__init__.py) | Modify | 加 `from .generate_audio import GenerateAudioExecutor` import(沿 image / mesh 模式;**不**自动注册到 registry,registry 注册在 `framework.run`) | §5.3 |
| [`src/framework/run.py`](src/framework/run.py) | Modify | `ExecutorRegistry` setup 段加 `registry.register(GenerateAudioExecutor(...))`(对照 generate_image / generate_mesh registration);**不**改 `loader.py`(F1 round-2:loader 仅 `Step.model_validate`,无 step-kind 表) | §5.4 |
| [`src/framework/runtime/failure_mode_map.py`](src/framework/runtime/failure_mode_map.py) | Modify | 加 `FailureMode.audio_worker_timeout` / `FailureMode.audio_worker_unsupported` enum(都 `Decision.abort_or_fallback`);`from_exception` 优先匹配 audio wrapped exc(在 generic ComfyWorker / WorkerTimeout 之前,沿 Phase 1 R4-F1 priority 修订) | §6.1 / §6.2 |
| [`src/framework/runtime/dry_run_pass.py`](src/framework/runtime/dry_run_pass.py) | Modify | `_check_comfy_reachability` line 142(approx) gate set 从 `{comfy/local, comfy/local-mesh}` 扩为 `{comfy/local, comfy/local-mesh, comfy/local-audio}` | §7.1 / §7.2 |
| [`config/models.yaml`](config/models.yaml) | Modify | 加 `models.comfy_local_audio`(`id: "comfy/local-audio"` + `provider: comfy_api` + `kind: audio` + `pricing: null` + `pricing_autogen.status: manual`)+ `aliases.audio_local`(`preferred: ["comfy_local_audio"]`);`providers.comfy_api` 不动 | §3.1 / §3.2 / §3.3 |
| [`tests/fixtures/test_models.yaml`](tests/fixtures/test_models.yaml) | Modify | 同步加 `comfy_local_audio` model + `audio_local` alias(用于 unit test 不污染 production yaml) | §3.4 |
| [`examples/comfy_local_smoke_audio.json`](examples/comfy_local_smoke_audio.json) | Create | 单 step bundle:`step.type="generate"` + `capability_ref="audio.t2a"`(F1 round-2 真实 Step 模型);`provider_policy.models_ref="audio_local"`;`spec.comfy_workflow="Audio_Workflows/audio_stable_audio_example"`;`comfy_params.text=...`;**no** `comfy_image_param_key`,**no** `depends_on` 上游;`retry_policy.max_attempts=2`(顶层 Step 字段,F2 round-2) | §8.1 |
| [`probes/provider/probe_comfy_audio.py`](probes/provider/probe_comfy_audio.py) | Create | opt-in via `FORGEUE_PROBE_COMFY_AUDIO=1`;模块顶层零副作用;真跑 ComfyAgentWorker.generate_audio 拿 FLAC bytes,validate magic + emit `[OK]`/`[FAIL]`/`[SKIP]` ASCII;输出落 `demo_artifacts/<date>/probes/provider/probe_comfy_audio/<HHMMSS>/`;exit 0=OK/SKIP,1=FAIL | §9.1 / §9.2 |

### Test files(new fence + extension)

| File | Operation | Responsibility | tasks.md 锚点 | 估算 fence 数 |
|---|---|---|---|---|
| [`tests/unit/test_audio_worker.py`](tests/unit/test_audio_worker.py) | Create | AudioWorker ABC contract / 异常树 inheritance / AudioCandidate format whitelist / FakeAudioWorker FLAC magic bytes / num_candidates 参数 | §2.6 | 5 |
| [`tests/unit/test_comfy_subprocess.py`](tests/unit/test_comfy_subprocess.py) | Modify | 加 audio fence:capability dispatch(2)+ 三段表 audio 行(8)+ generate_audio 实装 + magic bytes 二次校验(7,F5 round-2)+ regression(2)| §4.4 | +14 |
| [`tests/unit/test_generate_audio_comfy.py`](tests/unit/test_generate_audio_comfy.py) | Create | executor dispatch(3)+ retry budget 三 except 块(3,F2 round-2)+ 异常 wrap(4)+ 持久化(3)+ ADR-007 边界(1) | §5.5 | 14 |
| [`tests/unit/test_workflow_loader.py`](tests/unit/test_workflow_loader.py) | Modify | 加 audio.t2a capability_ref dispatch + alias rejection(2,F1 round-2:fence 名 step_kind → capability_ref) | §5.6 | +2 |
| [`tests/unit/test_model_registry.py`](tests/unit/test_model_registry.py) | Modify | comfy/local-audio resolves via audio_local + audio_local alias kind=audio(2) | §3.5 | +2 |
| [`tests/unit/test_failure_mode_map.py`](tests/unit/test_failure_mode_map.py) | Modify | audio_worker_timeout / unsupported maps to abort_or_fallback;audio priority over generic(6,沿 Phase 1 R4-F1 sweep)| §6.3 | +6 |
| [`tests/unit/test_dry_run_pass.py`](tests/unit/test_dry_run_pass.py) | Modify | dry_run_probes_comfy_when_comfy_local_audio_in_routes(1) | §7.3 | +1 |
| [`tests/integration/test_example_bundles_smoke.py`](tests/integration/test_example_bundles_smoke.py) | Modify | comfy_local_smoke_audio loads with audio_local alias / capability_ref / no workflow_graph(1) | §8.2 | +1 |
| [`tests/unit/test_probe_framework.py`](tests/unit/test_probe_framework.py) | Modify | probe_comfy_audio default skip without optin / no import side effects(1-2) | §9.3 | +1-2 |

**Total fence delta estimate**:**+45 fence**(实际数以 `pytest -q` 实测为准,**不**硬编码)— 比 round-1 估算 +35 多 +10,因 F5 magic bytes mandatory gate 加 7 fence + F2 三 except 块拆分新加 1 fence。Post-change baseline ≈ 1234 + 45 = ~1279。

### Authorized auxiliary files(Documentation Sync Gate + change-internal evidence)

| File | Operation | Responsibility | tasks.md 锚点 |
|---|---|---|---|
| [`docs/requirements/SRS.md`](docs/requirements/SRS.md) | Modify | §3.6 FR-STORE-004 audio metadata 三件套(`format` / `duration_seconds` / `sample_rate`);§3.8 加 FR-WORKER-011(audio worker baseline + capability dispatch);§3.7 FR-MODEL-007 alias 列表第 11 项 `audio_local`;§7.3 TBD-002 lift + TBD-009 Phase 2 完成;version v1.6 → v1.7 | §10.1 |
| [`docs/design/LLD.md`](docs/design/LLD.md) | Modify | AudioCandidate 字段表 + AudioWorker ABC 描述 + GenerateAudioExecutor 算法 + 失败模式映射 audio_worker_* | §10.1 |
| [`docs/design/HLD.md`](docs/design/HLD.md) | Modify | ComfyUI 子系统 capability dispatch 表加 audio 行;新增 §X.Y AudioWorker 章节(类比 MeshWorker) | §10.2 |
| [`docs/testing/test_spec.md`](docs/testing/test_spec.md) | Modify | audio fence 索引(+45 fence)+ comfy_local_smoke_audio.json Level 1/2 acceptance entry | §10.2 |
| [`docs/acceptance/acceptance_report.md`](docs/acceptance/acceptance_report.md) | Modify | audio capability 验收行 Phase 2;TBD 矩阵更新(TBD-002 baseline 落地;TBD-009 Phase 2 完成)| §10.3 |
| [`CHANGELOG.md`](CHANGELOG.md) | Modify | Unreleased 节加本 change entry(15-20 行 bullets,沿 Phase 1 mesh 风格)| §10.3 |
| [`CLAUDE.md`](CLAUDE.md) | Modify | ComfyUI 接入段加 audio capability + audio_local alias + 「audio 路径不需要 FORGEUE_COMFY_INPUT_DIR」 + 模型权重首次拉提示 + **F6 Stable Audio Open license note**(commercial ≤ $1M revenue,Stability AI Community License,enterprise 自审)| §10.4 |
| [`AGENTS.md`](AGENTS.md) | Modify(视情况)| 若文件存在且有 ComfyUI section,同步加 audio capability 一段 | §10.4 |
| [`README.md`](README.md) | (no change)| 本 change 不强制更新(audio 不直接出现在 §4.3 提示词;沿 Phase 1 mesh 模式)| - |

### Change-internal evidence directory

| File | Operation | Responsibility |
|---|---|---|
| `notes/audio_subprocess_probe_20260503.md` | ✅ Created | F4 round-1 probe(static read of `runner.py::extract_outputs` + agent CLI list/params 实测;OQ-1/2/3 全部 RESOLVED;ComfyUI server offline 因此真跑 `comfyui_api run` 推到 §11 L2 evidence) |
| `notes/live_smoke_audio_<date>.md` | Create at §11.5 | L2 evidence 真实 FLAC / MP3 / WAV 落盘:command line / run_id / artifact_id / 文件大小 / magic bytes / 主观音频质量 spot-check(F-Plan-R3-D round-3 修订:duration 校验删除 — 与 design D10 + artifact-contract spec `duration_seconds=None always` 决策一致;留 follow-on `audio-metadata-parser` change 加) |
| `review/codex_design_review.md` | ✅ Created | codex /codex:adversarial-review verbatim 输出(plugin_task_id=bktgw8l62);6 finding(2 high + 4 medium)|
| `review/design_cross_check.md` | ✅ Created | Claude `## A`(冻结)+ `## B`(6 row Cross-check Matrix)+ `## C`(disputed_open: 0)+ `## D`(file:line independent verification);所有 6 finding accepted-codex |
| `verification/verify_report.md` | Create at S5(`/forgeue:change-verify`)| Level 0/1/2 verify_report;12-key audit frontmatter |
| `review/codex_adversarial_review_round_final.md` | Create at S6(`/forgeue:change-review`)| codex G11 mixed scope 终审 |

---

## Critical Path

1. **§2 AudioWorker baseline 新建**(commit 1)→ §4 ComfyAgentWorker 4-dict 扩 audio + generate_audio + magic bytes(commit 3)→ §5 GenerateAudioExecutor + ExecutorRegistry 注册(commit 4)→ §6 FailureModeMap audio mode(commit 5)
2. **§3 ModelRegistry config 扩展**(commit 2)可与 §4 / §5 并行(无依赖)
3. **§7 DryRunPass + §8 examples bundle + §9 probe**(commit 6 / 7 / 8)是 §4-§6 的下游 — fence 守门 examples bundle loader,所以 §8.2 fence 必须在 §8.1 bundle commit 同 commit 或 head commit 之前 head pass
4. **§10 Documentation Sync Gate**(commit 9-12)在所有 production code commit 之后
5. **§11 L2 evidence**(commit 13)— **HARD BLOCKER for archive**(F-Plan-2 round-2 plan 修订:沿 Phase 1 mesh archive gate 模式 [archive/2026-05-03-comfy-agent-cli-mesh-audio-video-adoption/execution/execution_plan.md:162-191](openspec/changes/archive/2026-05-03-comfy-agent-cli-mesh-audio-video-adoption/execution/execution_plan.md#L162) 显式锁定「HARD BLOCKER + 禁止 post-archive defer L2 evidence + 无 ComfyUI host 时 S5 标 blocked,不允许 archive」)。需要用户启 ComfyUI server + Stable Audio Open 模型权重缓存(首次 ~2GB);若 user 当前无 ComfyUI host,**S5 标 blocked**,在 `verification/verify_report.md` 显式记录 blocker reason(`l2_evidence_pending: comfyui_host_unavailable`),`/forgeue:change-finish` archive gate 阻断;`docs/acceptance/acceptance_report.md` Phase 2 audio 行**不**标通过,直到 L2 full pass 后 backfill。**禁止 post-archive defer L2 evidence**(沿 Phase 1 image change 已建立的先例)。
6. **§12 codex G6/G11 hooks** 在 §10 完成后跑;G11 mixed scope blocker resolved 后 → S5 阻断 release(若 L2 evidence 未到位)→ L2 full pass 后 archive

**关键依赖锁**(以下顺序不能改):
- §2.6(test_audio_worker.py 5 fence)必须在 §2.1-§2.5 production AudioWorker baseline 之后(否则 fence 引用不存在的 class)
- §4.4(test_comfy_subprocess.py +14 audio fence)必须在 §4.1-§4.3 production 之后
- §5.5(test_generate_audio_comfy.py 14 fence)必须在 §5.1-§5.4 production 之后
- §6.3(test_failure_mode_map.py +6 fence)必须在 §6.1-§6.2 之后
- §8.2(test_example_bundles_smoke.py +1 fence)必须在 §3 ModelRegistry 扩 + §5 ExecutorRegistry 注册 + §8.1 bundle 之后(否则 loader 无法解析 audio_local alias)

---

## Round-1 codex review writeback summary(committed before this plan)

6 finding 全部 accepted-codex,writeback 已落到 contract artifacts(working tree,等 user 授权 commit):

| Finding | Severity | Writeback target | 状态 |
|---|---|---|---|
| F1 — step.kind 错(实际 Step.type=StepType.generate + capability_ref) | high | `specs/runtime-core/spec.md` 整段重写;`specs/examples-and-acceptance/spec.md` Scenario 修;`specs/provider-routing/spec.md` 2 处 Scenario 修;`tasks.md` §5.2-§5.6 / §8.1 改;`proposal.md` Impact 段改 | ✅ writeback complete in working tree |
| F2 — retry/wrap 伪代码 bare raise + 单 except 全 retry | high | `design.md` D9 三 except 块拆分(对照 `generate_mesh.py:160-172`);`tasks.md` §5.2 同步重写 + §5.5 fence 加 deterministic short-circuit | ✅ writeback complete in working tree |
| F3 — AudioCandidate.duration_seconds 顶层 vs metadata 冲突 | medium | `design.md` D5 反转(顶层 ACCEPTED);`specs/artifact-contract/spec.md` Scenario 改(顶层字段读);`tasks.md` §2.2 加 `\| None = None` | ✅ writeback complete in working tree |
| F4 — OQ-1/2/3 frozen as REQUIRED spec | medium | `tasks.md` §1.5 完成 probe(static read of `runner.py::extract_outputs`);§1.5b non-blocking implementation 阶段补全;`design.md` OQ-1/2/3 标 RESOLVED;`specs/provider-routing/spec.md` Step 5 改 duration/sample_rate=None always;`notes/audio_subprocess_probe_20260503.md` 新建 | ✅ writeback complete in working tree |
| F5 — magic bytes gate 缺失 | medium | `design.md` D10 反转(强制);`specs/provider-routing/spec.md` 加 Step 4 magic bytes second-pass;`tasks.md` §4.2 加 magic bytes 伪代码 + §4.4 加 7 fence | ✅ writeback complete in working tree |
| F6 — Stable Audio Open license note 未记 | medium | `design.md` Risks 表加 license 行;`tasks.md` §10.4 CLAUDE.md license disclaimer | ✅ writeback complete in working tree |

`forgeue_change_state.py --writeback-check` exit 0,`drifts: []`;frontmatter `writeback_commit: pending` 待 user 授权 commit 后填真实 hash。

---

## Risks(implementation 阶段需关注)

| Risk | Mitigation |
|---|---|
| §1.5b implementation probe 实测可能与 §1.5 静态阅读结论偏差(如 `extract_outputs` 实际 stdout JSON shape 与 source 不同) | 走 round-2 design / spec / tasks 修订(沿 Phase 1 R5 D10 模式);本 change 在 §11 L2 evidence 之前不 archive |
| ACE-Step custom node 用户机器未装 → Stable Audio default 跑不通时无 fallback | D11 已选 Stable Audio Open 作为主推 manifest 因为 `CLIPTextEncode` + `KSampler` 标准节点;ACE-Step 仅作 fallback debug;debug log 留 ACE-Step 路径 |
| F2 retry/wrap 三 except 块 implementation 时漏掉某个 inner exception 类型 | §5.5 fence 显式覆盖 timeout / unsupported / generic 三路 + deterministic short-circuit fence(不 retry)+ wrap with `from exc` chain fence 守门 |
| F5 magic bytes 校验对 mp3 frame sync 误判(MPEG layer 1/2/3 sync bytes 多变体) | §4.2 伪代码列出 `0xFF 0xFB / 0xFA / 0xF3 / 0xF2` 4 种常见 MPEG-1 / MPEG-2 layer III sync;若 implementation 期间发现遗漏(如 layer II `0xFD`),round-2 修订 design D10 + spec |
| ExecutorRegistry 注册 order 影响:`framework.run` 注册 audio executor 必须在 ExecutorRegistry init 之后 + Orchestrator 启动之前 | §5.4 显式说明,与 generate_image / generate_mesh 注册同位置 |
| Live smoke L2 evidence 跑不通(用户 ComfyUI server / 模型权重未就绪) | F-Plan-2 round-2 plan 修订:**L2 evidence HARD BLOCKER for archive**(沿 Phase 1 mesh 模式)— S5 标 blocked,`verification/verify_report.md` 显式记录 blocker reason,`/forgeue:change-finish` archive gate 阻断;`docs/acceptance/acceptance_report.md` Phase 2 audio 行**不**标通过直到 L2 full pass。**禁止 post-archive defer**(无 ComfyUI host 操作者不可推进 archive)|
| Stable Audio Open license 限制对企业用户 | F6 round-2 已加 design Risks + CLAUDE.md note;用户可切 manifest 绕过 |

---

## Open Questions remaining(post-round-1)

无。所有 6 finding accepted-codex + writeback 完成;OQ-1/2/3 通过 round-1 probe 解锁(详见 `notes/audio_subprocess_probe_20260503.md`)。

若 implementation 阶段 §1.5b 真跑 probe 发现 `runner.py` 静态阅读结论与实际 stdout JSON 偏差,走 round-2 codex review + design / spec / tasks 修订(沿 Phase 1 R2-R5 节奏)。
