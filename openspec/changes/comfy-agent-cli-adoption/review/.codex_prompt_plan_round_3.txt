# Target

OpenSpec change comfy-agent-cli-adoption S3 plan-stage ROUND 3 review (validates Q1-Q3 sweep + source-of-truth statement after round 2 plan codex feedback).

# Context

Round 2 plan codex (commit 656f7e2 post) found FIXED-CORRECTLY 1/4 plus 3 Q-findings. User chose Y (sweep all + source-of-truth statement). Sweep writeback at commit ed68e9f covers:

- Q1 critical fix: micro_tasks Task 2 (Step 2.1-2.7) full rewrite — providers.comfy_api uses api_key_env+api_base placeholder only (no kind/scripts_dir/python_exe/default_lifecycle); models.comfy/local has REQUIRED id; loader needs no schema extension; 3 fences renamed (placeholder_parses / model_id_missing_raises / alias_resolves_via_registry); commit message stripped of subprocess_cli claim
- Q2 high fix: tasks.md §3 header → "commit 3"; §4 → "commit 4"; §5 → "commit 2" (StepContext提前) with explicit cross-references and full commit chain order
- Q3 high fix: micro_tasks Step 6.1 fence list updated (23 names snapshot); renamed test_dry_run_skips_probe_when_no_comfy_api_in_routes → no_comfy_local_in_routes; explicit note that spec is canonical source — implementer must re-read spec before commit 6
- Source-of-truth statement: execution_plan.md + micro_tasks.md + tasks.md headers add "★ CONTRACT IS THE SOURCE OF TRUTH ★" — when these derived views diverge from contract (proposal/design/tasks/specs/*/spec.md), prefer the contract; transfers final authority to the contract

This round 3 plan codex validates whether the Q1-Q3 sweep eliminated plan-vs-contract drift sufficiently AND whether the source-of-truth statement is well-formed enough to absorb residual minor drift through implementer discretion (rather than requiring a round-4 sweep cycle).

# Files to read first

Round 2 plan evidence:
- review/codex_plan_review.md (round 1 plan codex, 4 P-findings)
- review/.codex_prompt_plan_round_2.txt (round 2 prompt)
- review/plan_cross_check.md (round 1 plan cross-check, frozen ## A + ## B/C/D)
- task output bvnwqv3xq for round 2 plan codex (3 Q-findings: Q1 critical micro_tasks subprocess_cli, Q2 high tasks.md commit numbering, Q3 high fence list mismatch)

Current post-sweep state (commit ed68e9f):
- execution/execution_plan.md (header has source-of-truth statement; File Structure + Task Group Map post-rework)
- execution/micro_tasks.md (header source-of-truth; Task 2 fully rewritten; Task 3 + 5 commit-order warnings; Task 6/Step 6.1 23-name fence list with spec-canonical note)
- tasks.md (header source-of-truth; §3/§4/§5 commit numbering 3/4/2)
- specs/provider-routing/spec.md + specs/runtime-core/spec.md + specs/probe-and-validation/spec.md (post-design-stage round 3 + post-plan-stage round 1 fixes)

# Round 3 plan-stage adversarial focus

QUESTION 1 — Did the Q1-Q3 sweep ACTUALLY fix each finding?

Per-finding verdict (fixed-correctly / fixed-with-caveat / not-actually-fixed):
- Q1 critical (Task 2 yaml/loader/fence sweep)
- Q2 high (tasks.md §3-§5 commit numbering)
- Q3 high (micro_tasks fence list + name)

End with counter "FIXED-CORRECTLY: N/3".

QUESTION 2 — Is the source-of-truth statement (header inserts in 3 plan files) well-formed?

Specifically:
- Does it correctly identify contract authority files (proposal / design / tasks / specs/*/spec.md)?
- Does it tell implementers what to do when plan diverges from contract (prefer contract)?
- Does it document the drift_decision protocol fallback for new gaps surfaced during apply?
- Is it visible enough at the top of each file to be noticed?

QUESTION 3 — Are there NEW R-findings introduced by the sweep?

Beyond Q1-Q3, look for issues introduced. Especially:
- Did Q1 (Task 2 rewrite) introduce any inconsistency with Task 3 ComfyAgentWorker which DOES read env config?
- Did Q2 (tasks.md commit numbering) leave any §6/§7/§8/§10 commit numbers wrong?
- Did Q3 (fence list 23 names) miss any fence that the current spec actually requires?
- Does the source-of-truth statement actually reduce risk, or is it documentation-only (vague-needs-rework)?

If new findings, use R prefix (R1, R2, ...) to distinguish from F/G/H/P/Q rounds.

End with counter + recommendation (proceed-to-apply / rework-plan-again / abort-and-restructure). The user has explicitly chosen Y (proceed if Q1-Q3 fix verified, accept residual drift via source-of-truth) — the threshold for "proceed-to-apply" should be: Q1-Q3 truly fixed (FIXED-CORRECTLY: 3/3 or 2/3 with mitigation) AND no new R-finding worse than medium severity.

Do NOT re-raise design-stage F/G/H findings or plan-stage P findings (closed by writeback).
